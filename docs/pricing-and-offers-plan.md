# خطة الأسعار المتعددة والعروض

كُتبت 2026-09-24 بطلب صاحب المشروع: «البيع بأكثر من سعر، مثل سعر تجار الجملة والتجزئة والقطاعي،
ونظام العروض والمجموعات على الأصناف - بطريقة قوية ومرنة». هذا هو البند 3 في `product-plan.md` §1
(«العروض على المنتجات»)، واتّسع ليشمل شرائح الأسعار، لأن كل عرض يُحسب على سعر شريحة.

**الحالة: المراحل أ (§10) وب (§11) وج (§12) مبنية - الحد الأدنى النافع كاملًا - ود وهـ لم تبدآ.** **وقراراتها حُسمت في اليوم نفسه** (§8): اعتمد صاحب المشروع
التوصيات كلها، وقرّر أن **العروض إضافة مدفوعة** في ملف المنتج (§6.1)، وأن يُفتح البند **قبل** البند 1
(سيرفر التفعيل).
اقرأها قبل لمس `type_price`، أو أعمدة `sel_price1..3`، أو نصف الخصم في `InvoiceSaveService`، أو أي
شيء تحت `features/pricing` أو `features/offers` حين يوجدان.

---

## 0. الطلب كما فُهم

ثلاثة أسئلة مختلفة، ولكل واحد منها مكانه في النموذج:

| السؤال | الجواب في هذه الخطة |
|---|---|
| بكم أبيع **لهذا المشتري**؟ تاجر جملة، تاجر تجزئة، مستهلك قطاعي | **شريحة السعر**: قائمة ثابتة يُسجَّل فيها سعر كل صنف بكل وحدة. تُحدَّد بالعميل. (§2) |
| بكم أبيع **الآن، وبهذه الكمية**؟ خصم 10%، «3 بـ 100»، اشترِ 2 وخذ 1 | **العرض**: قاعدة مؤقتة تُنتج خصمًا على السطر، ولا تغيّر السعر المسجَّل. (§3) |
| «المجموعات» | معنيان، والخطة تغطي الاثنين باسمين مختلفين حتى لا يختلطا: **مجموعة الأصناف** (الرئيسية والفرعية في الشجرة) كهدف لعرض - «خصم على كل المنظفات»؛ و**الطقم**: أصناف تُباع معًا بسعر واحد - «طقم رمضان». (§3، ق-ع١٢) |

والفرق بين الشريحة والعرض هو أساس الخطة كلها: **الشريحة تجيب «من يشتري»، والعرض يجيب «متى وكم».**
سعر الكمية («من 12 قطعة بسعر أقل») عرض لا شريحة، لأنه يتعلق بالكمية لا بالمشتري.

وهناك محور ثالث موجود أصلًا ولا يُخلط بهما: **الوحدة**. القطعة والكرتونة لكلٍّ سعرها في كل شريحة
(V6). فسعر الصنف مصفوفة:

| عصير 250 مل | قطاعي (شريحة 1) | تجزئة (شريحة 2) | جملة (شريحة 3) |
|---|---|---|---|
| القطعة (الأساسية) | 10.00 | 9.50 | 9.00 |
| الكرتونة (12) | 115.00 | 110.00 | 105.00 |

خانة فارغة (صفر) في صف الكرتونة تعني «سعر القطعة × 12» كما اليوم. هذا كله **موجود في القاعدة**؛ ما
ينقصه موصوف في §1.

---

## 1. الموجود قبلها (مقروء من الكود، لا مفترَض)

### 1.1 الشرائح

| الشيء | أين | حالته |
|---|---|---|
| ثلاثة أسعار بيع للصنف | `items.sel_price1/2/3` (V1) | موجودة؛ **الأولى وحدها** تُلزَم فوق التكلفة في شاشة الصنف (`ItemsService.requireSellAboveBuy`) |
| ثلاثة لكل وحدة | `items_units.sel_price/2/3` (V6) | الصفر = «سعر الصنف × المعامل» |
| أسماء الشرائح | `type_price`، ثلاثة صفوف `سعر1`، `سعر2`، `سعر3` | **لا شاشة تعدّلها**: `SelPriceItemService.update` بلا مستدعٍ، وحدث `SelPriceNamesChanged` لا يُنشر أبدًا. فكل تركيب يرى «سعر1، سعر2، سعر3» ما لم يعدّلها أحد بـSQL |
| قراءة الأسماء | `SelPriceItemService.getIntegerStringHashMap` | **بالموضع** `get(0..2)` من `loadAll` **بلا ORDER BY**، لا بالمعرِّف |
| شريحة العميل | `custom.price_id` → `type_price` (مفتاح أجنبي) | موجودة، والمورد بلا شريحة (صحيح: الشراء بالتكلفة) |
| اختيار السعر في الفاتورة | `InvoiceScreenController` (`priceTypeByNameId = t3NameData.priceId(party)`) ثم `SalesInvoice.getItemsPrice` (switch: 2 و3 وإلا 1) | الشريحة **من العميل وحده**؛ لا خانة لتغييرها على الفاتورة |
| الشريحة المحفوظة | — | **لا تُحفظ** على رأس الفاتورة ولا السطر: إعادة الطباعة أو التقرير لا يعرف بأي قائمة سُعِّرت الفاتورة |
| صنف بلا سعر في الشريحة | — | السعر يخرج **صفرًا** و`InvoiceLineService` يرفض السطر برسالة «سطر غير صالح»، لا «لا سعر جملة لهذا الصنف» |
| حدّ التكلفة | `InvoiceLineService.requireSalePrice`، `InvoiceLineEditService.requireNotBelowCost` | دائمًا مفعَّل للبيع، **على السعر لا على الصافي**: الخصم يستطيع إنزال الصافي تحت التكلفة بلا كلمة |
| السعر اليدوي | خانة السعر في الفورم وخلية السعر في الجدول | يُكتب أي سعر فوق التكلفة، ولا شيء يسجّل أنه خالف القائمة |
| «حدّث سعر الصنف أثناء الكتابة» | `invoice.update.price` → `InvoiceItemCatalogService` | يكتب في شريحة العميل؛ ويترك الوحدة التي لها سعرها |
| ملء الشرائح جماعيًا | شاشة أسعار الوحدات (`features/unitprices`) | تعدّل الأربعة سعرًا سعرًا؛ **لا قاعدة** مثل «الجملة = التجزئة − 5%». محرّر الأصناف الجماعي يعدّل الشريحة 1 وحدها |
| قراء الشريحة 1 وحدها | الملصقات، تقييم الجرد، تقرير شذوذ الأسعار، فلتر الكتالوج، الميزان | مقصود في أغلبها (التقييم بالشراء أصلًا)، والملصقات تحتاج اختيار شريحة |
| كشك الأسعار | `price.check.price.tier` لكل جهاز | يعمل |
| كود ميت | `DialogCashPaid.showPriceSelectionDialog` | منتقي شريحة لكل صنف، بلا مستدعٍ |
| الصلاحيات | `sel.price.update` (مُعلنة، يقرؤها `update` الذي لا يُستدعى) | `sel.price.show` و`.delete` أُزيلتا لأن لا شيء يقرؤهما (`permissions-plan.md` §2.2) |

### 1.2 الخصم

- **خصم السطر مبلغ لا نسبة**، يُكتب بعد إضافة السطر (`InvoiceLineEditService.editDiscount`)، و**بلا حدّ
  أعلى**: يُرفض السالب وحده، فخصم أكبر من السطر نفسه يجعل صافيه سالبًا. (عطل صغير وُجد أثناء القراءة؛
  يُصلَح في المرحلة ب لأن المحرك يحتاج السقف نفسه.)
- **دمج الصنف المكرر** يضيف الكمية ويتجاهل خصم السطر الجديد، ويُبقي خصم السطر القديم **مبلغًا ثابتًا**
  على كمية كبرت (`InvoiceLineService.add`).
- **خصم الفاتورة** مبلغ في `txtOtherDiscount`؛ خيار النسبة معطَّل، و`DiscountType` لا يُخزَّن.
- **رأس الفاتورة `total` = صافي السطور بعد خصوماتها**، و`discount` = الخصم الإضافي فوقها
  (`InvoiceSaveService`). و`document_profit` = `(total - discount) - SUM(total_buy_price)`.

**وهذا السطر الأخير هو ما تقوم عليه الخطة**: أي خصم يُكتب على السطر يدخل الربح صحيحًا، ويُرَدّ في
المرتجع بنسبته (`ReturnCostResolver` يشترط أن يأخذ سطر المرتجع حصة الخصم بنسبة كميته)، ويُطبع على
الإيصال وA4 (فيهما عمود خصم للسطر)، ويُنقص أساس عمولة المندوب (`total - discount`) - **كله بلا تعديل
view واحد**.

### 1.3 العروض والأطقم

- **لا شيء.** لا جدول ولا كود ولا مفتاح ترجمة لعرض أو هدية أو كوبون.
- `items_package` (V1) و`items.item_has_package` و`sales.item_has_package`: **لا يقرؤها شيء** غير دمج
  الأصناف والمسح. بلا شاشة وبلا أثر مخزني (`warehouse-plan.md` يقول ذلك). الطقم في هذه الخطة **لا
  يُبنى عليها** (ق-ع١٢).
- سقف خصم المندوب (V73) يجمع خصومات السطور والرأس ويقارنها بالإجمالي قبل الخصم.

---

## 2. القرارات - شرائح الأسعار (ق-س)

### ق-س١ — الشرائح ثلاث، مخزَّنة في أعمدتها، وتُقال في مكان واحد
الأعمدة الثلاثة على الصنف والوحدة **تبقى هي المخزن**. طلب صاحب المشروع ثلاثة بالضبط (جملة، تجزئة،
قطاعي)، و24 ملفًا يقرأ هذه الأعمدة اليوم (الكتالوج، الكشك، الملصقات، شاشة أسعار الوحدات، شاشة الصنف،
الإكسل، الـDAOs الأربعة...). تحويلها إلى جدول `item_price(item, unit, tier, price)` بلا حدّ يعيد كتابة
كل قارئ منها لسؤال لم يطرحه عميل بعد.

ما يتغيّر: الرقم «3» والـswitch المكرر في أربع كلاسات يصيران **كلاسًا واحدًا** (`features/pricing/PriceTier`
و`PriceResolver`)، فإن احتاج محلٌّ شريحة رابعة يومًا كان التغيير في مكان واحد. **والشريحة قابلة
للإيقاف** (`type_price.is_active`): محل يبيع بسعرين لا يرى ثالثًا في أي كومبو. الشريحة 1 لا تُوقَف (ق-س٣).

> **قُرِّر 2026-09-24: ثلاث.** البديل - قوائم بلا حد في جدول - رُفض بكلفته (~24 قارئًا وأسبوع إضافي)، ويبقى
> ممكنًا يومًا لأن الرقم صار يُقال في مكان واحد.

### ق-س٢ — الفاتورة تحمل شريحتها، منسوخة عليها
`total_sales.price_tier_id` (و`total_sales_re`): **الشريحة التي سُعِّرت بها الفاتورة**، تُنسخ لحظة الحفظ
كما ينسخ المستند سعر صرفه - لا يُقرأ من العميل لاحقًا، لأن نقل العميل من «قطاعي» إلى «جملة» غدًا لا
يعيد تسعير فاتورة الأمس. NULL لكل فاتورة قبل الهجرة.

**والافتراضي شريحة العميل، وتغييرها على الفاتورة بصلاحية** `sales.price.tier.change`: خانة في رأس
الفاتورة بجوار العميل، مقفلة لمن لا يملك المفتاح. تغييرها يعيد تسعير السطور التي أخذت سعرها من القائمة،
ويترك السطر الذي كُتب سعره يدويًا وسطر المرتجع المأخوذ من فاتورته، ويقول كم سطرًا ترك. والصلاحية
**جديدة ولا تُمنح لأحد عند الترقية** غير مدير النظام: القدرة لم تكن موجودة، فلا أحد يخسر شيئًا.

### ق-س٣ — صنف بلا سعر في الشريحة يُقال، لا يصير صفرًا
اليوم يخرج السعر صفرًا ويُرفض السطر برسالة لا تقول السبب. **قُرِّر 2026-09-24**: يرجع إلى **الشريحة 1** - وهي
الوحيدة التي تُلزمها شاشة الصنف فوق التكلفة، أي السعر الذي يملكه كل صنف - **وعلامة على السطر** تقول
«بسعر القطاعي: لا سعر جملة لهذا الصنف»، **وتقرير** «أصناف بلا سعر في شريحة نشطة» يصلح البيانات من
مصدرها. (البديل المرفوض: رفض بجملة تسمّي الصنف والشريحة - يوقف البيع عند الكاشير لخطأ في البيانات.)

### ق-س٤ — سعر القائمة والسعر المحصَّل حقيقتان
`sales.list_price`: ما قالته القائمة لهذا السطر، بجوار `price` الذي حُصِّل. سعر يُكتب يدويًا تحت سعر
القائمة **خصم متخفٍّ** لا يراه أي تقرير اليوم - وهو بالضبط ما يسأل عنه صاحب محل جملة: «هل يبيع الكاشير
بسعر الجملة لعميل قطاعي؟». فيصير:
- تقرير «بيع تحت سعر القائمة»: من، ومتى، وكم.
- إعادة التسعير في ق-س٢ تعرف السطر المكتوب يدويًا (`price <> list_price`) بلا علم في الذاكرة.
- **وقُرِّر 2026-09-24**: صلاحية `sales.price.below.list` لكتابة سعر تحت القائمة، **تُمنح عند الترقية
  لكل من يملك `sales.create`** - طريقة V79 - فلا يخسر أحد شيئًا، ثم يسحبها المحل ممن يريد.

### ق-س٥ — الشريحة تُملأ بقاعدة تكتب الأسعار، ولا تُحسب عند البيع
«الجملة = التجزئة − 5%، مقرَّبة لأقرب ربع جنيه» أو «= التكلفة + 12%». القاعدة **تكتب الأعمدة** بمعاينة
وتأكيد، في شاشة أسعار الوحدات (التي تحفظ اليوم بقفل تفاؤلي وتدقيق)، ولا تُحسب لحظة البيع. لو حُسبت
لحظة البيع لصار للسعر تعريفان: المكتوب والمحسوب، ولما عرف أحد أيهما قُرئ - وهو العيب الذي أغلقته خطط
الخزينة والأطراف. القاعدة المحفوظة على الشريحة (`type_price.rule_*`) تُستعمل أيضًا **اقتراحًا رماديًا**
في شاشة الصنف عند كتابة سعر القطاعي، كما يقترح `UnitPriceSuggestion` سعر الكرتونة اليوم.

### ق-س٦ — سعر الكمية عرض، لا شريحة
«من 12 قطعة بـ 9 بدل 10» يجيب عن **الكمية**، لا عن المشتري؛ فهو عرض من نوع «سعر الكمية» (المرحلة ج).
هذا يُبقي الشريحة جوابًا عن سؤال واحد.

---

## 3. القرارات - العروض (ق-ع)

### ق-ع١ — العرض لا يغيّر سعرًا مسجَّلًا؛ ينتج خصمًا على السطر
السطر يبقى بسعر قائمته، والعرض يكتب **خصمًا** عليه. وبجوار عمود الخصم عمودان: `offer_id` (أي عرض)
و`offer_discount` (كم من الخصم منه). والنتيجة من §1.2: **الربح والمرتجع والإيصال وقائمة الدخل وعمولة
المندوب وأرصدة العملاء كلها صحيحة بلا تعديل view**. ما يضاف هو تقرير يقرأ `offer_id` ليقول ماذا كسب
المحل وماذا أعطى.

### ق-ع٢ — خصم العرض يُوزَّع على السطور التي كسبته، بنسبة قيمتها
عرض يمسّ أكثر من سطر («شامبو + بلسم»، «طقم رمضان») يُوزَّع خصمه على سطوره **بنسبة قيمة كل سطر**،
مقرَّبًا HALF_UP، **والكسر المتبقي على أكبر سطر** - قاعدة `ReturnHeaderDiscount` نفسها («خصم لا مالك
له بين السطور يُقسَم بالقيمة؛ أي توزيع آخر اختراع»). ولماذا هذا مهم: **المرتجع يعمل بلا قاعدة جديدة.**
`ReturnCostResolver` يشترط اليوم أن يأخذ سطر المرتجع حصة خصم سطر المصدر بنسبة كميته، فمرتجع جزء من
عرض يرُدّ ما دُفع فيه بالضبط. **قُرِّر 2026-09-24: بالنسبة**؛ والبديل المرفوض - «استرداد العرض» - في المثال 4
من §5.

### ق-ع٣ — لا سطر بصفر: الهدية سطر بسعرها وخصمها
السطر بسعر صفر مرفوض اليوم (`price <= 0`)، ويبقى مرفوضًا. الهدية سطر بسعر قائمتها وحصتها من خصم
العرض. فيتحرك المخزون، وتُسجَّل تكلفتها، **ويظهر ثمن العرض في الربح** - وهي الحقيقة: الهدية كلّفت المحل
بضاعة.

### ق-ع٤ — محرك واحد، دالة نقية، يحكم مرتين
`features/offers/OfferEngine`: بلا قاعدة وبلا JavaFX.
المدخل: سطور الفاتورة (الصنف، الوحدة، الكمية بالوحدة الأساسية، سعر القائمة، المجموعة الفرعية والرئيسية)،
والسياق (نوع المستند، الشريحة، التاريخ، العملة)، والعروض السارية.
المخرج: لكل سطر (`offer_id`، `offer_discount`)، ولكل عرض سطر شرح، و**تلميحات** («أضف قطعة واحدة
لتأخذ الثالثة مجانًا»)، وسطور هدية مقترحة.

- **الشاشة** تشغّله بعد كل تغيير في السطور، معاينةً.
- **الحفظ** يشغّله ثانية داخل المعاملة (`OfferGuard` في `persist`) **قبل حجز رقم الفاتورة** - فالعدّاد لا
  يرجع - ويرفض إن اختلفت النتيجة عمّا على الشاشة، بجملة تسمّي العرض («انتهى عرض كذا؛ راجع الفاتورة»).
  نمط `DelegateDiscountGuard` و`ReturnGuard`: الحفظ هو الحَكَم، والشاشة تعرض ما سيقبله.
- العروض السارية تُحكم **بتاريخ المستند**، كسعر الصرف الساري.

### ق-ع٥ — عرض واحد لكل وحدة مبيعة، وترتيب ثابت
لا تراكم: كل قطعة يأخذها عرض واحد على الأكثر. ترتيب الحكم ثابت ومكتوب: **الأطقم، ثم «اشترِ وخذ»، ثم
عروض الكمية، ثم عروض السعر، ثم عرض إجمالي الفاتورة على ما بقي مؤهَّلًا**. وعند التعارض داخل النوع:
الأولوية المكتوبة على العرض، ثم الأنفع للعميل، ثم الأقدم. المحرك **جشع ومحدَّد**: لا يبحث عن أفضل
توليفة ممكنة بين كل العروض (مسألة تركيبية)، ويقول ذلك في javadoc، ولكل ترتيب اختبار.

### ق-ع٦ — هدف العرض ونطاقه
- **الهدف**: صنف، أو صنف بوحدة بعينها، أو مجموعة فرعية، أو رئيسية، أو كل الأصناف - **مع استثناءات**
  («كل المنظفات عدا المسحوق»).
- **النطاق**: كل الشرائح أو بعضها (عرض للمستهلك لا يصل تاجر الجملة)، وأيام الأسبوع اختياريًا.
- **النسبة تُحسب على سعر قائمة السطر** (سعر شريحته). و**سعر العرض لا يرفع سعرًا أبدًا**: الخصم =
  `max(0, سعر القائمة − سعر العرض)`؛ تاجر جملة سعره أقل من سعر العرض لا يأخذ منه شيئًا.
- الكمية في شرط العرض تُكتب بوحدة (قطعة أو كرتونة) وتُقارَن **بالوحدة الأساسية** (`ItemUnits.toBase`)، فكرتونة
  و3 قطع تُعدّ 15 قطعة. وعرض الكمية لا يمسّ سطر الميزان (وزن لا قطع)؛ النسبة والمبلغ يمسّانه.

### ق-ع٧ — العرض المستعمَل تاريخ
بمجرد أن يسمّيه سطر محفوظ: **شروطه لا تُعدَّل** (النسبة، السعر، الهدف) - يُوقَف ويُنشأ غيره، كقاعدة
العمولة. يُسمح بتقصير نهايته إلى ما لا يسبق آخر استعمال، وبإيقافه. **ولا يُحذف**؛ `DeleteRegistry` يعلن
`sales.offer_id` بلا CASCADE. المسودة التي لم تُستعمل تُحذف بصلاحيتها.

**والمستند المحفوظ يحتفظ بعروضه**: تعديل فاتورة قديمة يعيد تشغيل المحرك بالعروض السارية في تاريخها
**مضافًا إليها العروض المسجَّلة على سطورها** ولو أُوقفت بعدها - كما يحتفظ المستند بسعر صرفه ما دام يومه
وطرفه لم يتغيرا. تصحيح ملاحظة على فاتورة الشهر الماضي لا يسحب منها عرضها.

### ق-ع٨ — خصم العرض قرار المحل، لا قرار المندوب
`DiscountCeiling` يحكم على `discount - offer_discount`: عرض بـ20% لا يرفض فاتورة مندوب سقفه 10%.
**وقُرِّر 2026-09-24**: سطر عليه عرض لا يأخذ خصمًا يدويًا فوقه - العرض يحلّ محله، والشاشة تقول ذلك في سطر الحالة -
وخصم الفاتورة الإضافي يبقى كما هو ويحكمه سقف المندوب. (البديل المرفوض: يُجمعان ويحكم السقف اليدويَّ وحده.)

### ق-ع٩ — العرض تحت التكلفة يُحذَّر منه عند إنشائه
حدّ التكلفة اليوم على السعر لا على الصافي، والعرض لا يغيّر السعر. **قُرِّر 2026-09-24**: شاشة العرض تسرد الأصناف
التي سيصير صافيها تحت تكلفتها **قبل الحفظ** وتطلب تأكيدًا صريحًا، ولا يُرفض البيع - «رز بخسارة لجلب
الزبون» قرار تجاري مشروع، ويُتخذ مرة عند إنشاء العرض لا عند كل فاتورة.

### ق-ع١٠ — حدود العرض
- **حدّ للفاتورة الواحدة** («حتى 3 قطع للعميل»).
- **حدّ كلي** («أول 100 كرتونة»): `OfferGuard` يقفل صف العرض `FOR UPDATE` ويجمع ما بيع منه ناقص ما
  رُدّ، **قبل** أن يقفل `InvoiceStockGuard` صفوف الأصناف - ترتيب قفل واحد في كل مكان: العروض ثم الأصناف.
  فاتورتان على جهازين لا تتجاوزان الحدّ معًا.

### ق-ع١١ — أين تعمل العروض
- **فاتورة البيع الجديدة** على الشاشتين (العادية والسريعة)، فالمحرك يُستدعى من `InvoiceScreenController`
  المشترك لا من إحداهما.
- **المرتجع** لا يُطبَّق عليه محرك: سطره يأخذ `offer_id` وحصته من `offer_discount` من سطر المصدر.
  والمرتجع الحر بلا عرض.
- **الشراء**: لا. «بونص المورد» (اشترِ 10 وخذ 1) يغيّر التكلفة لا الخصم، وهو من بند الصيدليات - سجل الأفكار.
- **المستند بعملة أجنبية**: لا في المرحلة ب (**قُرِّر 2026-09-24**؛ تُضاف حين يطلبها محل)؛ الشاشة تقول ذلك في الشارة التي تسمّي العملة.

### ق-ع١٢ — الطقم عرض، لا صنف
«طقم رمضان» = عرض من نوع `BUNDLE`: مكوّنات (صنف، وحدة، كمية) بسعر إجمالي، وله **باركود اختياري**
إن مُسح أضاف المكوّنات سطورًا ثم وزّع المحرك خصمه عليها (ق-ع٢). المخزون يتحرك لكل مكوّن، والتكلفة
تكلفتها، والمرتجع يرُدّ مكوّنًا بعينه بنسبته.

**لماذا لا يكون الطقم صنفًا مركّبًا** (وهو ما يوحي به `items_package` الميت): الصنف المركّب بلا سطور شراء،
فرصيده في `quantity_items_table` بلا معنى، وجعله يُشتق من مكوّناته **تعريف ثانٍ للرصيد** - القاعدة الأولى
في `warehouse-plan.md`. التجميع الحقيقي (تصنيع صنف من خامات) بند مختلف تمامًا، وليس في هذه الخطة.
و`items_package` يبقى كما هو؛ حذفه قرار منفصل.

### ق-ع١٣ — العروض للمحل، وكل الأجهزة ترى الشيء نفسه
العروض في القاعدة. حدث `OffersChanged` يُعلَن من الخدمة داخل معاملتها (`Announcer.SERVICE`) فيصل الأجهزة
الأخرى عبر `data_change`. كل جهاز يحتفظ بلقطة من العروض السارية (استعلام صغير) ويجدّدها على الحدث
وعند تغيّر اليوم؛ والحفظ يقرأ من القاعدة على أي حال (ق-ع٤)، فاللقطة القديمة تؤخر معاينة ولا تكتب رقمًا.

### ق-ع١٤ — الورقة تقول ما وفّره العميل
الإيصال وA4 يُبقيان عمود خصم السطر كما هو، ويضيفان تحت السطور **سطرًا لكل عرض** («عرض 3 بـ 100:
−40.00») **وسطر «وفّرت اليوم»**. عمود العرض في جدول الفاتورة يحمل اسمه المختصر، وبطاقة «خصم العروض»
في الذيل بجوار الخصم.

---

## 4. النموذج

### 4.1 المرحلة أ - الهجرة التالية (`V84` إن لم يسبقها غيرها)

```
type_price
  + is_active      TINYINT(1) NOT NULL DEFAULT 1   -- الشريحة 1 لا تُوقَف (قاعدة Java + CHECK على id=1)
  + rule_source    VARCHAR(10) NULL                 -- NULL / 'COST' / 'TIER'  (ق-س٥)
  + rule_tier_id   INT NULL                          -- حين المصدر شريحة
  + rule_percent   DECIMAL(7,3) NULL                 -- موجب زيادة، سالب نقص
  + rule_rounding  DECIMAL(8,2) NULL                 -- 0.05 / 0.25 / 0.50 / 1 / 5
total_sales, total_sales_re
  + price_tier_id  INT NULL  FK type_price           -- ق-س٢؛ NULL لما قبلها
sales
  + list_price     DECIMAL(14,2) NULL                -- ق-س٤؛ NULL لما قبلها
```

كل CHECK يقارن عمودًا يقبل NULL يكتب `IS NOT NULL` بنفسه (درس V70: CHECK يمرّ حين يُقيَّم NULL).
والصلاحيات لا تحتاج هجرة للإعلان؛ تحتاجها للمنح فقط: `sales.price.below.list` تُمنح في الهجرة نفسها لكل دور
وكل استثناء `ALLOW` يحمل `sales.create` - طريقة V79.

### 4.2 المرحلة ب وما بعدها

```
offer
  id, name (UNIQUE), kind, status ('DRAFT','ACTIVE','STOPPED'),
  starts_on DATE NOT NULL, ends_on DATE NULL, weekdays TINYINT NULL (قناع أيام),
  priority INT NOT NULL DEFAULT 0,
  percent DECIMAL(6,3) NULL, amount DECIMAL(14,2) NULL, offer_price DECIMAL(14,2) NULL,
  unit_id INT NULL,                 -- وحدة المبلغ أو السعر أو الكمية
  buy_quantity, get_quantity DECIMAL(14,3) NULL, get_percent DECIMAL(6,3) NULL,
  threshold DECIMAL(14,2) NULL,     -- عرض الفاتورة
  max_per_invoice, quantity_limit DECIMAL(14,3) NULL,
  barcode VARCHAR(200) NULL UNIQUE, -- الطقم
  notes, user_id, created_at, updated_at TIMESTAMP(6)   -- إصدار للتعديل المتفائل (V50/V52)
  + CHECK لكل نوع: ما يلزمه موجود وما لا يخصه NULL
offer_target
  id, offer_id (CASCADE: الهدف جزء من تعريف العرض), role ('QUALIFY','REWARD','COMPONENT'),
  scope ('ITEM','SUB_GROUP','MAIN_GROUP','ALL'), item_id, unit_id, group_id NULL,
  quantity DECIMAL(14,3) NULL, excluded TINYINT(1)
offer_price_tier (offer_id, price_tier_id)   -- لا صفوف = كل الشرائح
sales, sales_re
  + offer_id INT NULL  FK offer (بلا CASCADE)
  + offer_discount DECIMAL(14,2) NOT NULL DEFAULT 0
```

`kind`: `PERCENT`، `AMOUNT`، `PRICE` (المرحلة ب)؛ `QUANTITY_PRICE`، `BUY_GET` (ج)؛ `BUNDLE`، `INVOICE` (د).
أعمدة عادية بـCHECK لا JSON - كما كل جدول في المشروع، ولأن التقرير يجمعها في SQL.

**ما يجب إعلانه في المراجعة نفسها، وكل واحد له اختبار سيفشل إن نُسي:**

| الإعلان | لماذا | الاختبار |
|---|---|---|
| `offer_target.item_id` في `ItemReferenceRegistry` | دمج صنفين يجب أن ينقل أهداف العروض | `ItemReferenceRegistryTest` يقرأ المفاتيح من الهجرة - **بشرط أن تُضاف الهجرة إلى قائمة `SchemaForeignKeys`**، وإلا لا يراها (درس V75) |
| `offer_target.item_id`، `group_id`، `unit_id`، `sales.offer_id` في `DeleteRegistry` | حذف صنف أو مجموعة أو وحدة يسمّيها عرض، وحذف عرض مستعمَل | `DeleteRegistryTest`، و`UnitsDao.isInUse` يُحدَّث يدويًا |
| الجداول الثلاثة في `WipeCatalog` | «حذف البيانات» | `WipeCatalogTest` |
| أعمدة السطر في `DocumentTableSpec` | كل عبارة للسطور مثبتة حرفًا حرفًا | `DocumentDaoStatementsTest` |
| مشغّلات التدقيق على `offer` | من أنشأ العرض ومن أوقفه | في **آخر** `R__triggers.sql` (الاختبارات التي تبني schema أقدم تقطع الملف عند علامات سابقة) |
| مفاتيح الترجمة | كل رسالة ورفض | `MessageKeyArchitectureTest` |
| المفاتيح الجديدة في `PermissionGroup` | كل مفتاح في مجموعة واحدة | `PermissionCatalogArchitectureTest` |
| `OffersChanged` في `RemoteChangeTopics` بـ`SERVICE` | لا كتابة مزدوجة ولا موضوع صامت | `MultiDeviceRefreshArchitectureTest` |

---

## 5. المحرك بالأمثلة (محسوبة باليد، وكل مثال يصير اختبارًا)

**1 - نسبة على مجموعة.** «خصم 10% على المنظفات». صابون 3 × 40 = 120 ⇒ `offer_discount` 12.00، الصافي 108.

**2 - سعر الكمية.** «3 بـ 100»، السعر 40، الكمية 7 ⇒ مجموعتان (6 قطع): 2 × (120 − 100) = 40.
السطر 7 × 40 = 280، خصم 40، صافي 240 = 200 + 40 ✓. **وتاجر جملة سعره 32**: 3 × 32 = 96 < 100 ⇒ لا خصم (ق-ع٦).

**3 - اشترِ 2 وخذ 1، الصنف نفسه، بـ 30.** الكمية 3 ⇒ خصم 30، صافي 60. الكمية 5 ⇒ مجموعة واحدة، خصم 30،
صافي 120، وتلميح: «أضف قطعة تأخذ الثانية مجانًا».

**4 - شامبو (60) وبلسم هدية (40).** الإجمالي 100، الخصم 40 موزَّع بالقيمة: شامبو 24، بلسم 16. الصافي
36 + 24 = 60 ✓ = سعر الشامبو.
مرتجع الشامبو وحده ⇒ يُرَدّ 36 (حصته). العميل احتفظ ببلسم دفع فيه 24. **وبالبديل** («استرداد العرض»)
يُرَدّ 60 − 40 = 20 فقط، لأن البلسم لم يعد هدية. الأول بلا قاعدة جديدة في المرتجع، والثاني يحتاج قاعدة
جديدة في `ReturnGuard` تعيد تشغيل المحرك على الباقي بتاريخ الفاتورة. **قُرِّر الأول** (§8).
والربح إن كانت التكلفة 45 و30: 60 − 75 = **−15**، وتقرير العروض يقولها.

**5 - طقم رمضان بـ 150.** زيت 1 × 80، سكر 2 × 30 = 60، أرز 1 × 25 ⇒ 165، الخصم 15.
بالقيمة: زيت 80/165 × 15 = 7.2727 ⇒ 7.27؛ سكر 60/165 × 15 = 5.4545 ⇒ 5.45؛ أرز 25/165 × 15 = 2.2727 ⇒ 2.27.
المجموع 14.99، فالقرش الباقي على أكبر سطر: زيت **7.28**. المجموع 15.00 ✓.

**6 - إجمالي الفاتورة.** «من 1,000 فأكثر خصم 5%»، يُحكم على صافي السطور المؤهَّلة **بعد** عروضها. مؤهَّل
1,200 ⇒ 60 موزَّعة بالقيمة على تلك السطور، **لا في خانة خصم الفاتورة** - حتى يُرَدّ بنسبته كأي خصم سطر.

**7 - المندوب.** سقفه 10% على إجمالي 300: خصم عرض 40 + خصم يدوي على الفاتورة 20 ⇒ يُحكم على 20 وحدها
(ق-ع٨) فيمرّ.

---

## 6. أين يهبط كل تغيير

**`features/pricing`** (المرحلة أ، بلا JavaFX، ولكل كلاس اختبار):
`PriceTier`، `PriceTierCatalog` (الأسماء من `type_price` **بالمعرِّف**، النشطة فقط)، `PriceResolver` - يحلّ محل
`getItemsPrice` في الكلاسات الأربعة ونداءات `ItemUnits.sellPrice` المبعثرة، ويُرجع السعر **ومصدره** (سعر
الوحدة في الشريحة، سعر الصنف × المعامل، الرجوع إلى الشريحة 1) - `TierFillRule` (ق-س٥)، `MissingTierPrices`.

**`features/offers`** (ب إلى د، بالشروط نفسها): `Offer` (POJO)، `OfferKind`، `OfferTarget`، `OfferForm`
(يرمي مفاتيح لا جملًا)، `OfferQuery` (مثبت حرفًا حرفًا)، `OfferRepository`/`JdbcOfferRepository`،
`OfferService` (كل كتابة تبدأ بـ`require`)، `OfferEngine`، `OfferAllocation` (التوزيع والكسر)، `OfferGuard`،
`ActiveOffers` (اللقطة)، ثم `OfferReport`.

**الفاتورة:**
- `InvoiceScreenController`: خانة الشريحة (أ)، واستدعاء المحرك بعد كل تغيير في السطور، وعمود العرض،
  وبطاقة «خصم العروض»، والتلميحات في سطر الحالة على الشاشة السريعة (لا نوافذ: المسح التالي يغلقها غير مقروءة).
- `InvoiceLineService.add`: **الدمج يعيد تشغيل المحرك** بدل إبقاء خصم ثابت على كمية كبرت.
- `InvoiceLineEditService.editDiscount`: سقف = إجمالي السطر (§1.2)، والرفض على سطر عليه عرض (ق-ع٨).
- `InvoiceSaveService.persist`: `OfferGuard` بعد التحويل للأساسية وقبل `InvoiceStockGuard` (ترتيب القفل،
  ق-ع١٠)، وقبل حجز الرقم. `DelegateDiscountGuard` يحكم على الخصم ناقص خصم العرض.
- `ReturnCostResolver`/`ReturnableLineSelection`: يحملان `offer_id` وحصة `offer_discount` من سطر المصدر.
- `InvoicePrintDocument`، `InvoiceReceiptLayout`، `InvoicePdfLayout`: سطور العروض وسطر «وفّرت».

**الشاشات:**
- **«شرائح الأسعار»** (أ): الأسماء، والإيقاف، وقاعدة الملء. أخيرًا يقرأ أحدٌ `sel.price.update`.
  وتُقرأ الأسماء في شاشة أسعار الوحدات وجدول وحدات الصنف بدل التسميات الثابتة، ويُحذف `DialogCashPaid`
  الميت.
- **«العروض»** (ب): قائمة بشريط `ListToolbar` (الحالة، الفترة، النوع، نص)، وصف العرض يفتح في
  `RowDetailDrawer` أهدافه وأداءه، ونموذج تتغيّر خاناته بالنوع، وخانة **«جرّب»** (كمية ⇒ ما سيعطيه العرض)
  كما في شاشة قاعدة العمولة. يُصمَّم لـ1366×768 ويُصوَّر قبل أن يُسمّى «مُشاهَدًا».

**الصلاحيات** (لكل منها `permission.<key>` في الحزم الثلاث):

| المفتاح | ماذا | عند الترقية |
|---|---|---|
| `sales.price.tier.change` | تغيير شريحة الفاتورة | لا أحد (قدرة جديدة) |
| `sales.price.below.list` | سعر يدوي تحت القائمة (ق-س٤) | كل من يملك `sales.create` |
| `offer.show` | رؤية العروض وتقريرها | كل من يملك `items.show` |
| `offer.create` / `offer.update` | إنشاء، وتفعيل وإيقاف وتعديل ما لم يُستعمل | كل من يملك `items.update` |
| `offer.delete` | حذف مسودة لم تُستعمل (`DELETE` ⇒ CRITICAL) | كل من يملك `items.delete` |

وتقرير العروض يطلب `offer.show` مع `reports.show.sales`، وعمود الربح فيه `reports.show.profit` - بلا مفتاح
تقارير جديد. والمنح عند الترقية في الجدول **قُرِّر 2026-09-24**: من يملك صلاحية الأصناف المقابلة.

### 6.1 العروض إضافة مدفوعة (قُرِّر 2026-09-24)

العروض - المراحل ب إلى هـ - ميزة منتج جديدة `ProductFeatures.OFFERS`، **حاضرة فقط في ملف منتج وُقِّع بها**.
و**الشرائح (المرحلة أ) ليست منها**: الأسعار الثلاثة في قاعدة كل عميل منذ V1، وما تضيفه المرحلة أ إصلاح لما
يملكه فعلًا (اسم لا يُعدَّل، وشريحة لا تُحفظ، وسعر يخرج صفرًا)، فتبقى تحت ميزة الأصناف. هذه قراءة الخطة لا
قرار صاحب المشروع؛ إن أرادها في الإضافة تُنقل بسطر.

**الميزة الجديدة تصل اليوم من ثلاثة طرق، واثنان منها يعطيانها مجانًا** - قُرئ من الكود:

| الطريق | ما يحدث اليوم لمفتاح جديد | ما يلزم ليبقى مدفوعًا |
|---|---|---|
| ملف موقَّع بالإصدار 2 | غائب عن `enabledFeatures` ⇒ مطفأ | لا شيء - هذه هي الإضافة |
| ملف موقَّع بالإصدار 1 | `ProductProfileCodec` يفعّل **كل** مفتاح ليس في `VERSION_1_KEYS` | استثناء الإضافات، وإلا أخذها كل حامل ملف قديم |
| قاعدة بلا ملف (`LEGACY_FULL`) | `ProductProfile.legacyFull` = `catalog.keys()` كلها | استثناء الإضافات. وعد `LEGACY_FULL` أن الترقية لا تُخفي شاشة **قائمة**؛ ميزة جديدة لم تكن قائمة |

فالتنفيذ: `ProductFeatureDefinition` يحمل علامة «إضافة»، والطريقان الأخيران يستثنيان ما يحملها، واختبار يقول إن
`LEGACY_FULL` وقراءة ملف الإصدار 1 لا يفعّلان إضافة أبدًا، وقائمة الإضافات `{OFFERS}` مثبتة **في الاتجاهين** كما
القوائم الأخرى في المشروع. وفي أداة `AccountK-Product-Setup` تظهر الإضافات في قسم لها، **ولا تدخل أي نسخة
جاهزة ولا «كاملة»**: تُعلَّم باليد، فلا يُعطيها فنيٌّ لعميل لم يشترها لأنه اختار «كاملة». وعميل بلا ملف أصلًا
يشتري العروض بأول ملف يُوقَّع له - «كاملة» + العروض - فلا يخسر شاشة.

**وبلا الميزة**: زر العروض ومدخلها في فهرس التقارير مخفيان، و`OfferService` يسأل `ProductFeatureAccess.require`،
والمحرك لا يعمل على الفاتورة ولا `OfferGuard` عند الحفظ. **ومستند حُفظ بعروض يبقى كما هو**: تعديله بلا الميزة
لا يعيد حساب خصم العرض على سطوره ولا يمحوه - هو خصم مسجَّل كأي خصم - والمرتجع منه يأخذ حصته من سطر المصدر
كما في ق-ع١١. `ProductProfileWiringArchitectureTest` يعدّ الأزرار المربوطة بميزة، فسيتغير عدده.

---

## 7. المراحل

البند واحد، ومراحله تُشحن واحدة واحدة. كل مرحلة: هجرة تُجرَّب **على schema من الصفر وعلى نسخة بيانات
قائمة** قبل أن تُصدَّق، واختبار قبول مقيَّد على MySQL يبني schema مؤقتًا ويحذفه، والشاشة تُفتح وتُصوَّر
بمستخدم عادي - **لا المستخدم 1، الذي يتخطى كل صلاحية**.

| المرحلة | ماذا | التقدير | ما يثبتها على MySQL |
|---|---|---|---|
| **أ — الشرائح** | شاشة الأسماء والإيقاف، `PriceResolver`، الرجوع للشريحة 1 وتقرير الناقص، الشريحة على الفاتورة ومحفوظة عليها، `list_price` وتقرير «تحت القائمة»، قاعدة الملء في شاشة أسعار الوحدات، اختيار الشريحة في الملصقات | ~1 أسبوع | فاتورة لعميل جملة بصنف بلا سعر جملة؛ تغيير الشريحة بمفتاح وبدونه؛ الشريحة المحفوظة بعد نقل العميل لشريحة أخرى |
| **ب — المحرك وعروض السعر** | الجداول، `OfferEngine` بالأنواع الثلاثة (نسبة، مبلغ، سعر)، `OfferGuard`، شاشة العروض، عمود العرض وبطاقته، الإيصال وA4، الصلاحيات، سقف خصم السطر، **ميزة `OFFERS` إضافةً مدفوعة** (§6.1) | ~1.5 أسبوع | الأمثلة 1 و7 باليد؛ `document_profit` يساوي الحساب اليدوي؛ مرتجع جزء من فاتورة عرض يمرّ بكل حارس ويرُدّ حصته؛ الحفظ يرفض عرضًا انتهى والعداد لم يتحرك |
| **ج — الكمية والهدايا** | `QUANTITY_PRICE` و`BUY_GET` (الصنف نفسه وصنف آخر)، التلميحات، حدّ الفاتورة والحدّ الكلي | ~1 أسبوع | الأمثلة 2 و3 و4؛ **الحدّ الكلي بمعاملتين حقيقيتين** كما أُثبت قفل العملة الأساسية |
| **د — الأطقم وعرض الفاتورة** | `BUNDLE` بباركوده، `INVOICE` | ~1 أسبوع | المثالان 5 و6؛ مسح باركود الطقم يضيف المكوّنات ويتحرك مخزون كل واحد |
| **هـ — التقرير والتنبيهات والملصق والكشك** | «أداء العروض» (فواتير، كميات، خصم ممنوح، صافٍ، تكلفة، ربح، مرتجع، ومقارنة بالفترة قبله)، تنبيه «عرض ينتهي غدًا» و«صنف في عرض قارب النفاد»، ملصق بسعر العرض والسعر القديم مشطوبًا، سعر العرض على الكشك | ~1 أسبوع | التقرير يطابق `document_profit` للفواتير نفسها |

**المجموع ~5.5 أسابيع**، مقابل «~3 أسابيع» في `product-plan.md` للعروض وحدها - الفرق هو الشرائح (أ) والأطقم
وعرض الفاتورة (د) والتقرير (هـ). **الحد الأدنى النافع: أ + ب + ج (~3.5 أسابيع)** - الشرائح كاملة، وخصم
النسبة والمبلغ وسعر العرض، و«3 بـ 100» و«اشترِ وخذ». د وهـ يمكن أن ينتظرا طلب عميل.

---

## 8. قرارات صاحب المشروع

عُرضت عشرة قرارات بتوصياتها، و**حُسمت كلها 2026-09-24 إلا الأول**: اعتُمدت التوصيات كما هي، وقُرِّر
التاسع - الذي لم تكن له توصية.

| # | القرار | الحالة |
|---|---|---|
| 1 | **الترتيب**: هل يُفتح هذا البند قبل البند 1 (سيرفر التفعيل) أم بعده؟ البند 1 مسجَّل «التالي» وينتظر شراء دومين قبل S2، وقاعدة «بند واحد مفتوح» تمنع الاثنين معًا | **قبله** - قُرِّر 2026-09-24 بعد «أقرر لاحقًا» في اليوم نفسه، وفُتح البند بالمرحلة أ |
| 2 | عدد الشرائح (ق-س١) | **ثلاث**، قابلة للتسمية والإيقاف |
| 3 | صنف بلا سعر في شريحة العميل (ق-س٣) | **يرجع للشريحة 1 بعلامة على السطر**، وتقرير بالأصناف الناقصة |
| 4 | السعر اليدوي تحت القائمة (ق-س٤) | **`list_price` يُسجَّل دائمًا، و`sales.price.below.list` تُمنح لكل من يملك `sales.create` عند الترقية** ثم يسحبها المحل ممن يريد |
| 5 | مرتجع جزء من عرض (ق-ع٢، المثال 4) | **بالنسبة**: يرُدّ ما دُفع في القطعة، بلا قاعدة مرتجع جديدة |
| 6 | خصم يدوي على سطر عليه عرض (ق-ع٨) | **العرض يحلّ محله ويقول ذلك**؛ خصم الفاتورة الإضافي كما هو |
| 7 | عرض تحت التكلفة (ق-ع٩) | **تحذير بقائمة الأصناف عند إنشاء العرض**، ولا رفض عند البيع |
| 8 | العروض على مستند بعملة أجنبية (ق-ع١١) | **لا في المرحلة ب**؛ تُضاف حين يطلبها محل |
| 9 | العروض في ملفات المنتج الموقَّعة | **إضافة مدفوعة** - وما يلزم لذلك في الكود في §6.1 |
| 10 | من يُمنح صلاحيات العروض عند الترقية (§6) | **من يملك صلاحيات الأصناف المقابلة** |

قرار يُغيَّر لاحقًا يُكتب هنا بتاريخه وسببه، ولا يُمحى الأول - كما في `currency-plan.md` §4.

## 9. ما ليس في الخطة، عمدًا

| الفكرة | لماذا ليست هنا |
|---|---|
| الكوبونات ونقاط الولاء | تحتاج هوية العميل عند كل بيعة ورصيد نقاط - دفتر ثانٍ. سجل الأفكار |
| ساعات اليوم («من 5 إلى 7 مساءً») | تاريخ المستند يوم لا ساعة؛ وقت الإدخال لا يصلح شرطًا لفاتورة تُعدَّل. أيام الأسبوع في الخطة |
| سعر خاص لعميل بعينه | الشريحة تغطي الحالة الغالبة، وعرض مقصور على شريحة يغطي الباقي. سجل الأفكار |
| تغيير سعر مجدول («السعر الجديد من أول الشهر») | يقترب من ق-س٥؛ يُنظر فيه بعد المرحلة أ |
| بونص المورد | يغيّر تكلفة الشراء لا خصم البيع. بند الصيدليات |
| أفضل توليفة عروض رياضيًا | ق-ع٥: المحرك جشع ومحدَّد وموثَّق |
| الصنف المركّب والتجميع (`items_package`) | ق-ع١٢ |
| نسبة لخصم الفاتورة الإضافي (`radioRate` المعطَّل) | صغير ومستقل؛ يُصلَح مع أول لمسة للشاشة إن طُلب |

---

## 10. ما سلّمته المرحلة أ (2026-09-24)

فُتح البند قبل البند 1 بقرار صاحب المشروع (§8-1)، وبُنيت المرحلة أ كلها في يومها. **هذا ما بُني، وما اختلف عمّا
كُتب فوق، وما شُغِّل وما شوهد وما لم يُرَ.**

### 10.1 ما بُني

- **`V84__price_tiers.sql`**: `type_price.is_active` وقاعدة الملء (`rule_source`، `rule_tier_id`، `rule_percent`،
  `rule_rounding`) بـCHECK يكتب IS NULL / IS NOT NULL في كل فرع ومفتاح من الشريحة إلى نفسها؛
  `total_sales.price_tier_id` و`total_sales_re.price_tier_id` بمفتاحين إلى `type_price`؛ `sales.list_price`؛
  الصلاحيتان، و`sales.price.below.list` ممنوحة لكل دور وكل استثناء ALLOW يحمل `sales.create`. وفي آخر
  `R__triggers.sql` قسم V84: trigger يمنع إيقاف الشريحة 1 وملء شريحة من نفسها، وتدقيق كل تعديل على شريحة.
- **`features/pricing`**: `PriceTiers` (المكان الوحيد الذي يعرف أن الشريحة 2 هي `sel_price2` - كان `switch` في
  خمسة كلاسات)، `PriceResolver` و`ListedPrice` (سعر الوحدة في الشريحة، ثم سعر الصنف × المعامل، ثم **سعر
  الشريحة 1 بعلامة**)، `PriceTier` و`PriceTierCatalog` (الأسماء **بالمعرِّف**)، `PriceTierService` (الكتابة بـ
  `sel.price.update`)، `TierFillRule` و`TierFill` و`TierFillService` (الملء)، `TierReportService` (التقريران)،
  و`PriceTierForm`. ولكل كلاس اختبار.
- **الفاتورة**: السطر يحمل `listPrice` وعلامة الشريحة 1؛ `InvoicePriceTier` يقرر الشريحة المحفوظة ويسأل
  `sales.price.tier.change` و`sales.price.below.list` **قبل حجز الرقم**، ويكتب الشريحة بعد الرأس؛
  `PriceTierRepricing` يعيد تسعير ما بقي على سعر قائمته فقط. خانة الشريحة بجوار الطرف في الشاشتين، معطّلة
  لمن لا يملك المفتاح، وملاحظة التسعير بجوار الشارات في العادية وفي سطر الحالة في السريعة.
- **الشاشات**: «شرائح الأسعار» (قائمة «أخرى» في الأصناف) بثلاثة تبويبات - الشرائح، أصناف بلا سعر، بيع تحت
  سعر القائمة؛ أسماء الشرائح على أعمدة الأصناف وشاشة أسعار الوحدات ونموذج الصنف وكومبو العميل وكشك
  الأسعار؛ واختيار الشريحة في شاشة الملصقات (`PrintBarcodeModel.showTier`)، محفوظ لكل جهاز.
- **ما حُذف**: `SelPriceItemService` (قرأ الأسماء بالموضع من استعلام بلا ترتيب)، و`DialogCashPaid` (بلا مستدعٍ)،
  و`SelPriceNamesChanged` (لم يُنشر قط) - وحلّ محله `PriceTiersChanged` يعلنه الخدمة ويصل كل الأجهزة.

### 10.2 ما اختلف عن الخطة، ولماذا

| # | ما كُتب | ما بُني | لماذا |
|---|---|---|---|
| 1 | CHECK على `id = 1` | trigger في `R__triggers.sql` وقاعدة في `PriceTierService` | MySQL يرفض CHECK على عمود AUTO_INCREMENT (الخطأ 3818): كانت الهجرة ستفشل على كل تركيب |
| 2 | قاعدة الملء في شاشة أسعار الوحدات | زر «طبّق القاعدة» في صف الشريحة، عبر `TierFillService` | تلك الشاشة تعرض الأصناف متعددة الوحدات وحدها، وأغلب الأصناف بوحدة واحدة |
| 3 | القاعدة تكتب الأعمدة | والمعاينة تفتح على «الأصناف التي بلا سعر في هذه الشريحة فقط» | أول معاينة على البيانات الحقيقية كتبت فوق أسعار جملة كُتبت يدويًا؛ إلغاء الخيار يكتب فوقها بعد أن تقول المعاينة ذلك |
| 4 | الخطة لم تقل ماذا يحدث لعملاء شريحة موقوفة | يُسعَّرون بالشريحة 1 وتبقى شريحتهم محفوظة، والشاشة تسمّي عددهم قبل الإيقاف | الرفض كان سيُلزم نقلهم واحدًا واحدًا بلا أداة لذلك |
| 5 | خطورة الصلاحيتين لم تُذكر | MEDIUM، لا LOW المشتقة من آخر كلمة | سعر تحت القائمة وفاتورة كاملة بسعر الجملة كلاهما خصم |
| 6 | الاقتراح الرمادي للقاعدة في شاشة الصنف (ق-س٥) | **لم يُبنَ** | خانات الأسعار تبدأ بـ0.0 من `setTextFormatter`، فلا نص تلميح يظهر؛ يحتاج تصميمًا لا سطرًا |
| 7 | تقرير «تحت سعر القائمة» | تبويب في شاشة الشرائح، لا بطاقة في فهرس التقارير | بطاقة الفهرس تحتاج مدخلًا وفتحًا خاصًّا؛ تُضاف حين يُطلب |

### 10.3 ما شُغِّل على MySQL

- **`PriceTierDatabaseAcceptanceTest`**: ثماني حالات، **خضراء مرتين** على schema من الصفر في خادم MySQL 8.0.31
  خاص على منفذ جانبي، ولم يبقَ schema بعدها. تثبت: أعمدة V84 ومفاتيحها وCHECK والtrigger (1644 و3819) والمنح؛
  تسمية الشرائح عبر الخدمة وتبديل اسمين في حفظ واحد والتدقيق؛ فاتورة لعميل جملة بصنف بلا سعر جملة تُحفظ
  بسعر القطاعي وشريحتها 3 و`list_price`؛ رفض شريحة أخرى وسعر تحت القائمة بلا مفتاح **والعدّاد لم يتحرك**،
  وقبولهما بالمفتاح؛ تقرير «تحت القائمة» بالفرق (100 − 90) × 2 = 20؛ الشريحة تبقى مع الفاتورة بعد نقل
  العميل، وتعديلها يمرّ بلا مفتاح؛ تقرير «بلا سعر» ثم الملء يخرجه منه؛ وشريحة موقوفة لا تُختار من جديد ولو بالمفتاح.
- **V84 على نسخة بيانات التطوير** (V78 إلى V84، 1,841 صنفًا و6,500 سطر مبيعات): ثلاثة أدوار تحمل `sales.create`
  وثلاثة صارت تحمل `sales.price.below.list`، ولا أحد يحمل `sales.price.tier.change`؛ ولا رأس ولا سطر قديم
  كُتبت له شريحة أو سعر قائمة.

**وما وجده التشغيل وحده**: نص استعلام تقرير «تحت القائمة» كان يلتصق فيه آخر جزء بأول الذي يليه
(`ts.price_tier_idFROM`) - كتلة نص Java تحذف المسافة البادئة، واختبار الوحدة كان يقارن جزءًا مبنيًّا بالطريقة
نفسها فمرّ؛ صار يقارن العبارة كاملة. و`items_units` يسمّي وحدته `unit` لا `unit_id`. وكلاهما مرّ بكل اختبار وحدة.

### 10.4 ما شوهد على الشاشة

على نسخة بيانات التطوير في الخادم الخاص، بمستخدمَين عاديَّين (لا المستخدم 1): كاشير بدور
`DEFAULT_SALES_CASHIER` بلا `sales.price.tier.change`، ومدير يملكها. عند 1366×768:

- فاتورة بيع لعميل على «جملة»: الخانة تقول «جملة» ومعطّلة للكاشير؛ صنف بسعر جملة بـ70، وكرسي بلا سعر جملة
  بسعر القطاعي 300 بلون التحذير، والملاحظة «بسعر قطاعي: لا سعر جملة له».
- المدير يبدّلها إلى «قطاعي»: السطران يعاد تسعيرهما (70 → 75) وتزول العلامة، والملاحظة تقول العدد.
- الفاتورة السريعة عبر سطر الإدخال نفسه: الخانة بجوار العميل، والملاحظة في سطر الحالة.
- شاشة الشرائح بتبويباتها، ومعاينة «طبّق القاعدة» (1,844 سعرًا في 1,839 صنفًا، والأسعار الموجودة مستثناة)،
  وأعمدة أسعار الوحدات بأسماء الشرائح، والملصقات بسعر الجملة والرجوع إلى القطاعي.

**وما وجدته الشاشة وحدها**: عنوان «النسبة %» رمى `UnknownFormatConversionException` وظهر مفتاحًا خامًا -
`LanguageManager.getString` يمرّ كل قيمة على `String.format`؛ وملاحظة الشاشة كانت تقول إن القاعدة تُطبَّق من شاشة
أسعار الوحدات؛ ورسائل العدد («2 سطرًا») صارت بصيغة «...: 2».

**ما لم يُرَ**: الحفظ بزر الشاشة (جُرّب عبر الخدمة في اختبار القبول - النسخة تفرض الورديات)، وإعادة فتح فاتورة
محفوظة على الشاشة، ورفض سعر تحت القائمة في الخلية، وتأكيد إيقاف شريحة عليها عملاء، والكتابة الفعلية من زر
«طبّق القاعدة» (المعاينة شوهدت وأُلغيت؛ الكتابة في اختبار القبول)، والإنجليزية، والوضع الداكن، وطابعة ملصقات حقيقية.

### 10.5 شريحتان لم تُستعملا قط (قُرِّر 2026-09-25)

على بيانات التطوير **1,839 صنفًا من 1,841 بلا سعر في الشريحة 2، و1,840 بلا سعر في الشريحة 3، وكل العملاء
الـ147 على الشريحة 1**. V84 تُبقي الثلاث نشطة كما كانت، فتقرير «أصناف بلا سعر» كان يسرد الكتالوج تقريبًا كله إلى
أن يوقف المحل ما لا يبيع به أو يملؤه بقاعدة.

**القرار: لا يُوقَف شيء عند الترقية، والتقرير هو الذي يتغيّر.** وُزنت ثلاثة: الإبقاء كما هو؛ وهجرة توقف الشريحة
غير المستعملة - ورُفضت، لأن الشريحة الموقوفة تختفي من كل كومبو، و«غير مستعملة» لا يقولها جدول (محل يطبع
ملصقات بسعر الجملة ولا عميل على الجملة)، والشرط الآمن «لا عميل ولا سعر» لا يوقف شيئًا على بيانات التطوير نفسها
لأن صنفين لهما سعر 2؛ وتغيير ما يعدّه التقرير - وهو المأخوذ. `MissingPriceScope` يقرّر، بلا JavaFX وباختبار:

- **الشريحة 1 تُعدّ دائمًا** - كل صنف يجب أن يحملها، وهي ما يرجع إليه السعر الناقص.
- **شريحة نشطة عليها عميل نشط تُعدّ**، وشريحة نشطة **بلا عميل** لا تُعدّ، بل تُسمّى في سطر تحت الشريط مع الطريقين:
  إيقافها من تبويب الشرائح أو ملؤها بقاعدة. والشريحة الموقوفة لا تُعدّ ولا تُسمّى: لا أحد يُباع بها.
- **خانة «تشمل الشرائح التي لا عميل عليها»** بجوار البحث تعدّها كلها كما كان التقرير - تظهر فقط حين توجد شريحة
  كهذه. وعمود «بلا سعر في» يسمّي الشرائح المعدودة وحدها.

«عليها عميل» هو `activeCustomersByTier`، العدّ الذي يعرضه تبويب الشرائح نفسه. **لم يُحسب** فاتورة سُعِّرت بشريحة
بلا عميل عليها (بمفتاح `sales.price.tier.change`): إن كان المحل يفعل ذلك فالخانة تعرضها.

**شوهد** على schema من الصفر ببيانات العرض (ستة عملاء، 21 صنفًا، أُفرغت الشريحتان 2 و3 من 19)، بمستخدم عادي عند
1366x768: الكل على 1 ⇒ صفر وسطر يسمّي «سعر2» و«سعر3»؛ الخانة ⇒ 19؛ عميل على 3 ⇒ 19 كلها «سعر3» والسطر يسمّي «سعر2».
**لم يُرَ**: الإنجليزية والوضع الداكن.

---

## 11. ما سلّمته المرحلة ب (2026-09-24)

بُنيت المرحلة ب في اليوم نفسه بعد دمج أ (PR #67)، على الفرع `claude/offers-phase-b`. **هذا ما بُني، وما اختلف
عمّا كُتب فوق، وما شُغِّل وما شوهد وما لم يُرَ.**

### 11.1 ما بُني

- **`V85__offers.sql`**: `offer` (نوع واحد في الصف - نسبة أو مبلغ أو سعر - بـCHECK لكل نوع يكتب IS NOT NULL في
  كل فرع، وحالة DRAFT/ACTIVE/STOPPED، وتواريخ، وقناع أيام، وأولوية، ووحدة للمبلغ أو السعر، و`updated_at` إصدارًا)،
  `offer_target` (صنف، صنف بوحدة، مجموعة فرعية، رئيسية، كل الأصناف، وأيها مستثنى) و`offer_price_tier` -
  وكلاهما يُجرف مع عرضه؛ و`offer_id` و`offer_discount` على `sales` و`sales_re` بمفتاح **لا يُجرف** وCHECK
  (`offer_discount >= 0` ولا خصم عرض بلا عرض)؛ والصلاحيات الأربع ممنوحة لكل دور وكل استثناء ALLOW يحمل
  صلاحية الأصناف المقابلة. وفي **آخر** `R__triggers.sql` قسم V85: trigger يرفض تعديل شروط عرض سمّاه سطر،
  وتدقيق الإنشاء والتعديل والحذف.
- **`features/offers`**: `Offer` و`OfferTarget` و`Weekdays` (نماذج نقية)، `OfferEngine` (المحرك، دالة نقية)،
  `OfferGuard` (حكم الحفظ)، `OfferForm` (يرمي مفاتيح)، `OfferQuery` (كل عبارة، مثبتة حرفًا حرفًا)،
  `OfferRepository`/`JdbcOfferRepository`، `OfferService` (كل كتابة تبدأ بـ`require`، والإضافة تُسأل قبلها)،
  `OfferCostCheck` (تحذير ما تحت التكلفة، ق-ع٩). ولكل كلاس اختبار.
- **الفاتورة**: السطر يحمل `offerId` و`offerDiscount` واسم العرض للعرض فقط؛ `InvoiceOffers` يحوّل السطور إلى
  سطور المحرك ويكتب إجابته عليها ويحكم عند الحفظ؛ `InvoiceOfferPreview` هو ما تشغّله الشاشة بعد كل تغيير في
  السطور أو التاريخ أو الشريحة (في `InvoiceScreenController` المشترك، فالشاشتان معًا)، ويُجدَّد على `OffersChanged`.
  عمود «العرض» آخر أعمدة الجدول، و«خصم العروض» في الذيل تحت عدد الأصناف بتلميح يسرد كل عرض.
- **الحفظ**: `InvoiceOffers.judge` بعد قرار الشريحة وقبل حجز الرقم - يقرأ العروض السارية في تاريخ المستند
  والمسجَّلة على سطوره المحفوظة، ومجموعات الأصناف **من القاعدة** لا من الصنف المحمَّل. و`DelegateDiscountGuard`
  يُسأل الآن بعده، على الخصم ناقص خصم العروض.
- **الخصم اليدوي**: `editDiscount` يرفض خصمًا على سطر عليه عرض ويسمّي العرض، ويرفض خصمًا أكبر من قيمة
  السطر - في الخلية وعند الحفظ (§1.2).
- **المرتجع**: `ReturnCostResolver.carryTheOffer` يكتب على سطر المرتجع عرض سطر المصدر وحصته من خصمه بنسبة
  الكمية - من سطر المصدر، لا من الشاشة - والمرتجع الحر بلا عرض.
- **الورقة**: `InvoicePrintDocument.offers` و`saved()`؛ سطر لكل عرض ثم «وفّرت اليوم» تحت الملخص في A4 والإيصال
  معًا (`InvoicePdfLayout.offerRows`)، على بيع وصله عرض فقط.
- **الشاشة**: «العروض» في قسم الأصناف: قائمة بشريط `ListToolbar` (النص والحالة في الشريط، والنوع والأيام في
  اللوحة)، وأزرار الصف (تفاصيل، تعديل، تفعيل/إيقاف، حذف معطّل لعرض مستعمَل)، ودرج `RowDetailDrawer` بأهداف العرض
  وما أعطاه (فواتير، سطور، خصم، ما رُدّ، الصافي، أول وآخر استعمال)، ونموذج (`OfferFormDialog`) تتغير خانة قيمته
  بالنوع، بمحرر أهداف وخانة «جرّب العرض».
- **الإضافة المدفوعة** (§6.1): `ProductFeatureDefinition.addOn`، و`ProductFeatureCatalog.keysWithoutAddOns()` هو
  ما يأخذه `LEGACY_FULL` وقراءة ملف الإصدار 1 والنسخة «كاملة»؛ وفي أداة ملف المنتج قسم «الإضافات المدفوعة»
  بخانة غير معلَّمة لا تمسّها النسخ الجاهزة.
- **الإعلانات**: `ItemReferenceRegistry` (نقل أهداف العروض في الدمج)، `DeleteRegistry` (الصنف والوحدة والمجموعتان
  وقاعدة `OFFERS` جديدة)، `WipeCatalog` (العروض تُمسح مع الأصناف)، `SchemaForeignKeys`، `PermissionGroup.OFFERS`،
  `OffersChanged` بـ`Announcer.SERVICE`.

### 11.2 ما اختلف عن الخطة، ولماذا

| # | ما كُتب | ما بُني | لماذا |
|---|---|---|---|
| 1 | «النسبة تُحسب على سعر قائمة السطر» (ق-ع٦) | على **السعر الذي يحصّله السطر** | سعر يُكتب تحت القائمة خصمٌ بالفعل؛ نسبة من القائمة فوقه تعطي الفرق مرتين، وسعر عرض يُحسب من القائمة ينزل تحت سعر العرض. على سطر بسعر قائمته - وهو الغالب - الرقمان واحد |
| 2 | `group_id` واحد للمجموعتين | عمودان، `sub_group_id` و`main_group_id`، لكل منهما مفتاحه | عمود يشير إلى جدولين لا يحمل مفتاحًا، فحذف مجموعة يسمّيها عرض يمرّ بلا كلمة |
| 3 | أعمدة ج ود في V85 | أعمدة ب وحدها، وقيود أنواعها | عمود لا يقرؤه شيء وCHECK يَعِد بنوع لم يُبنَ؛ كل مرحلة تضيف أعمدتها |
| 4 | لم تقل الخطة متى يبدأ العرض | **لا يبدأ قبل يوم كتابته أو تفعيله** | الحفظ يعيد تشغيل المحرك على فاتورة معدَّلة بعروض تاريخها (ق-ع٧)، فعرض مؤرَّخ في الماضي يظهر على فواتير الشهر الماضي حين تُصحَّح. وما بقي - ووجده اختبار القبول: تعديل فاتورة **اليوم** يأخذ عرضًا بدأ اليوم بعدها إن أعطى أكثر، وهو ما تقوله ق-ع٧ حرفيًا |
| 5 | «شروطه لا تُعدَّل» في Java | وفي القاعدة أيضًا: `offer_terms_update` | عبارة من خارج البرنامج لا تعيد كتابة ما أُعطيته فاتورة محفوظة؛ نمط تجميد احتساب العمولة |
| 6 | حذف المسودة التي لم تُستعمل | حذف **أي** عرض لم يُستعمل | `DeleteRegistry.OFFERS` يرفض المستعمَل بسطره؛ عرض فُعِّل ولم يبِع شيئًا لا يختلف عن مسودة |
| 7 | تحذير ما تحت التكلفة لكل من ينشئ عرضًا | لمن يملك `show.column.buy.price` فقط | التحذير يعرض التكلفة، ومن لا يراها في قائمة الأصناف لا يراها هنا |
| 8 | «بطاقة خصم العروض في الذيل بجوار الخصم» | رقم في الذيل تحت عدد الأصناف، وعمود «العرض» آخر أعمدة الجدول | الخانة الفارغة في شبكة الذيل هناك في الشاشتين، والأعمدة بعد عمود الحذف لا تحرّك فهارس يقرؤها الكود |
| 9 | «وفّرت اليوم» بلا تعريف | كل خصم على الورقة (السطور والفاتورة)، على بيع وصله عرض فقط، والأرقام بلا إشارة | ما وفّره العميل عن الأسعار المطبوعة؛ ومحل بلا عروض يطبع ما كان يطبعه. والسالب في سطر من اليمين يقع على الجانب الآخر من الرقم |
| 10 | مفتاح الميزة `offers` | `addons.offers` | `FeatureKey` يشترط نقطة؛ أول تشغيل للاختبارات أسقط تهيئة `ProductFeatures` كلها |
| 11 | لم تذكر شاشة المرتجع | لا عمود عرض فيها؛ الحفظ يكتب العرض والحصة من سطر المصدر | المرتجع لا يُطبَّق عليه محرك (ق-ع١١)، والحصة رقم من المصدر لا قرار من الشاشة |
| 12 | مسح العروض لم يُذكر | تُمسح مع الأصناف في «حذف البيانات» | العرض تصريح عن أصناف؛ هدف مستقل يحتاج قسمًا وبطاقة في شاشة المسح بلا فائدة |

### 11.3 ما شُغِّل على MySQL

- **`OfferDatabaseAcceptanceTest`**: تسع حالات، **خضراء مرتين** على schema من الصفر في خادم MySQL 8.0.31 خاص على
  منفذ جانبي، ولم يبقَ schema بعدها. تثبت: جداول V85 ومفاتيحها وCHECKها (3819) والمنح (كل مفتاح عرض = عدد
  حاملي مفتاح الأصناف المقابل)؛ عرض يُكتب عبر الخدمة بمفتاحه ويُرفض بدونه ويُرفض بتاريخ أمس، ويُدقَّق؛ **المثال 1
  باليد**: صابون 3 × 40 يأخذ 12.00 على السطر بـ`offer_id` و`offer_discount`، والرأس 208 = 108 + 100، و
  `document_profit` = 208 − 135 = 73؛ الحفظ يرفض فاتورة لا تحمل عرضًا يصلها وفاتورة برقم عرض مختلف **والعدّاد لم
  يتحرك**، والخدمة والـtrigger (1644) يرفضان تعديل شروط عرض مستعمَل؛ **مرتجع قطعة من ثلاث** يمرّ بكل حارس ويُكتب
  بسعر 40 وخصم 4.00 وعرضه وحصته 4.00 وتكلفة البيع، و`document_profit` للمرتجع صافٍ −36 وتكلفة −25 وربح −11؛ **المثال 7**: سقف
  مندوب 10% على 300، عرض «40 خصم» (يغلب 10% لأنه يعطي أكثر) وخصم يدوي 20 يمرّ، و35 يُرفض والعدّاد لم يتحرك؛ عرض
  أُوقف يُرفض على فاتورة جديدة ويبقى مع فاتورة أُعطيَته عند تعديلها؛ وحذف عرض مستعمَل يُرفض، ومسودة تُحذف
  بأهدافها وشرائحها، وصنف يسمّيه عرض لا يُحذف من تحته؛ وعبارتا تحذير ما تحت التكلفة - بالوحدة الأساسية، وبوحدة
  مسمّاة بسعر الصنف × 12 حين لا سعر لها - تُرفضان بلا مفتاح التكلفة وتسردان الصابون وحده بعده.
- **وما وجده التشغيل وحده**: أول تشغيل فشل في حالة «العرض الموقوف يبقى مع فاتورته» لأن عرض «40 خصم» من المثال 7
  كان ما زال مفعَّلًا، فأخذه تعديل الفاتورة - وهذا صحيح بقاعدة ق-ع٧ (البند 4 في §11.2)، والخطأ في الاختبار.
- **والجيران على الخادم نفسه**: عمل المرحلة لمس الحفظ والمرتجع وسقف المندوب وعرض سطور المبيعات، فشُغِّلت الأصناف
  المقفولة التي تقرأها وخرجت كلها خضراء: العملات (12)، مستندات العملة (11)، سقف الخصم (7)، نشاط المندوب (9)
  وتفصيله (6)، المصروفات (19، تبني schema أقدم وتقطع الـtriggers عند علامة V64)، قائمة الدخل (5)، مبيعات الأصناف
  (5)، الملخص (5)، أسباب المرتجعات (5)، ومصدر المرتجع (1) ومستودع المرتجعات (14) على schema مُرحَّل من الصفر ولم
  يبقَ فيه صف، و`PriceTierDatabaseAcceptanceTest` (8).

### 11.4 ما شوهد على الشاشة

على schema مُرحَّل من الصفر في الخادم الخاص محمَّل بمحل الدليل الوهمي (`docs/manual/demo-data.sql`)، بمستخدمَين
عاديَّين: مدير بدور `DEFAULT_INVENTORY` (وأخذ المفاتيح الأربع من V85 لأنه يحمل مفاتيح الأصناف) وكاشير بدور
`DEFAULT_SALES_CASHIER` (أخذ `offer.show` وحده). ملف المنتج في المشغّل «كل الميزات» - قاعدة بلا ملف موقَّع تعطي
`LEGACY_FULL` بلا الإضافة، وهو المقصود. عند 1366×768:

- **شاشة العروض**: أربعة عروض كُتبت عبر الخدمة (10% على مجموعتي المنظفات عدا أريال، كوكاكولا بـ18 للقطاعي فقط،
  5 جنيه على الزيوت الجمعة والسبت، ومسودة 15% تبدأ بعد أسبوع)، مرتبة المفعَّلة أولًا؛ الدرج بأهداف العرض وما أعطاه؛
  والنموذج بمحرر الأهداف، وخانة «جرّب العرض» تقول لقطعة فيري «القيمة 42.00، الخصم 4.20، الصافي 37.80».
- **فاتورة البيع العادية**: فيري 3 × 42 خصمها 12.60 من العرض، كوكاكولا 6 × 20 خصمها 12.00 (سعر العرض 18)، أريال
  المستثنى والأرز بلا شيء؛ عمود «العرض» آخر الجدول، و«خصم العروض 24.60» في الذيل.
- **الفاتورة السريعة** عبر سطر الإدخال بالباركود: 4.20 و2.00 و«خصم العروض 6.20»، وسطر الإدخال لم يُمَسّ.
- **الورقة**: A4 مرسومة من ملف PDF بـPDFBox والإيصال 80مم بمسار Java2D الذي تُرسَل به الطابعة الحرارية: سطر لكل
  عرض ثم «وفّرت اليوم 26.00» (24.60 على السطور و1.40 على الفاتورة).

**وما وجدته الشاشة وحدها، وأُصلح:** الشاشة بلا لوحة ألوان، فعنوانها أبيض على خلفية فاتحة (`.screen-offers` صارت
في السِمتين، وردية)؛ وتاريخا العرض داخل سطر عربي في رأس الدرج انقلبا («08-10-2026 ← 24-09-2026») - صارا سطرين في
الشبكة؛ وزر واحد «تفعيل أو إيقاف» بعلامة ✓ على عرض مفعَّل لا يقول ماذا سيفعل - صار زرين، كل منهما يعمل حين يصح؛
وعمود العرض في الفاتورة ضيّق على اسم طويل - اتّسع.

### 11.5 ما لم يُرَ

(بعد فحص §11.6.) ملف منتج موقَّع بالإضافة فعلًا (مسار `ProductProfileCodec` مختبر وحدويًا؛ لا مفتاح خاص هنا)،
وأداة ملف المنتج بقسم الإضافات، والإنجليزية، والوضع الداكن، وطابعة حقيقية، ونافذة ما تحت التكلفة **من زر حفظ
النموذج** (هي الدالة نفسها التي شوهدت من زر التفعيل). و`ButtonType.CANCEL` في النموذج ونافذة التكلفة يظهر بلغة
الجهاز كما في كل حوارات البرنامج.

### 11.6 الفحص بعد الدمج (2026-09-24)

ما تركته §11.5 الأولى غير مرئي، مُشغَّلًا من أزرار الشاشات نفسها. خادم MySQL خاص، schema مُرحَّل من الصفر محمَّل
بمحل الدليل الوهمي، وثلاثة مستخدمين عاديين: مدير العروض (`DEFAULT_INVENTORY` + الكاشير، ويرى التكلفة)، وكاشير
(`DEFAULT_SALES_CASHIER`)، ومشرف (`DEFAULT_SALES_MANAGER`) لتعديل فاتورة محفوظة - **الكاشير لا يملك `sales.update`،
فزر الحفظ على فاتورة محفوظة معطَّل له، وهذا صحيح**. مشغّل يضغط الأزرار ويجيب الحوارات، ويقرأ كل نتيجة من MySQL
لا من الشاشة. ضُغط «حفظ» وحده، لا «حفظ وطباعة». **اثنان وعشرون فحصًا، كلها خضراء في تشغيلتين بعد الإصلاح**
(وثالثة بعد عمود الشريحة):

- **أزرار صف العروض**: «تفعيل» مسودة 90% على فيري يفتح نافذة ما تحت التكلفة؛ الرجوع منها يترك العرض مسودة،
  والتأكيد يفعّله؛ «إيقاف» يسأل ثم يوقف؛ «حذف» عرض لم يُستعمل يسأل ثم يحذفه وتختفي معه القائمة إلى عرضين؛ وعلى
  عرض استُعمل زر الحذف والتفعيل معطَّلان والإيقاف يعمل.
- **فاتورة بالزر**: خصم يدوي 5 على سطر فيري (عليه عرض) يُرفض برسالة تسمّي العرض ويبقى 12.60؛ خصم 100 على الأرز
  (سطره 68) يُرفض «خصم السطر أكبر من قيمته»؛ وخصم 3 يُقبل. ثم «حفظ» → شاشة الدفع → حفظ: الفاتورة 1011 في القاعدة
  = 113.40 + 108 + 92 + 65 = **378.40**، فيري بعرضه 12.60، كوكاكولا 12.00، أريال بلا عرض، والأرز بخصمه اليدوي 3
  وبلا عرض؛ `document_profit` 378.40 − 332 = 46.40 - كلها باليد.
- **إعادة الفتح والتعديل**: الفاتورة تُفتح بعروضها على سطورها و«خصم العروض 24.60»؛ فيري 4 بدل 3 تأخذ 16.80 على
  الشاشة، والحفظ يسأل ثم يقول «تم الحفظ» ويغلق النافذة، والقاعدة 416.20 وربح 50.20، والأرز بقي بخصمه اليدوي.
- **الفاتورة السريعة بالزر**: فيري 2 وكوكاكولا 1 بالباركود → 93.60 في القاعدة بخصمي العرضين 8.40 و2.00.

**وما وجده الفحص، وأُصلح:**

1. **شاشات الجهاز نفسه لا تسمع تغيير عرض.** بعد تفعيل عرض أو إيقافه أو حذفه بقيت القائمة على حالها - زر التفعيل
   مضاء على عرض مفعَّل، والعرض المحذوف فيها - وفاتورة مفتوحة على الجهاز نفسه بقيت بعروضها القديمة حتى تُغلق. السبب:
   `OfferService` يعلن التغيير في `data_change` للأجهزة الأخرى، والمرحِّل يتخطى عمدًا صفوف جهازه، ولا شيء كان ينشره
   على الـ`EventBus` هنا. **صورتا المرحلة ب لم تريا ذلك لأن العروض كُتبت قبل فتح الشاشات.** الإصلاح نمط شاشات
   الخزينة: `OffersController` ينشر `OffersChanged` بعد كل كتابة (النموذج، التفعيل، الإيقاف، الحذف). والثغرة نفسها في
   المرحلة أ: `PriceTiersChanged` (شاشة الأصناف تعرض أسماء الشرائح) و`ItemsChanged` بعد قاعدة الملء - ينشرهما
   `PriceTiersController` الآن. **وقاعدة جديدة** في `MultiDeviceRefreshArchitectureTest`
   (`anEventAScreenListensForIsPublishedOnThisMachine`): حدث تستمع له شاشة يُبنى في مكان غير نداء `announce` - فشلت
   على `OffersChanged` قبل الإصلاح وخضراء بعده. وفحص جديد في المشغّل: فاتورة مفتوحة تُسقط عرض كوكاكولا لحظة إيقافه من
   شاشة العروض على الجهاز نفسه.
2. **نافذة ما تحت التكلفة سعّرت فيري بـ40.00** وسعره 42.00: الصنف يُسرد على أسوأ شريحة (الثالثة، 40)، والشريحة لم
   تُكتب. صار لها عمود.

**ولوحظ ولم يُصلَح، لأنه ليس من العروض**: خانة «المدفوع نقدًا» في فاتورة نقدية مفتوحة للتعديل تكتب `378.4` والأرقام
حولها `378.40` - منسّق `Utils.setTextFormatter` على خانة تكتبها الشاشة (القاعدة في CLAUDE.md، «The two party
screens»)، وقديم قبل العروض.

---

## 12. ما سلّمته المرحلة ج (2026-09-25)

بُنيت على الفرع `claude/offers-phase-c` بعد فحص §11.6: سعر الكمية («3 بـ 100») و«اشترِ وخذ» من الصنف نفسه ومن
صنف هدية، والتلميحات، وحدّ الفاتورة والحدّ الكلي. **بها يكتمل الحد الأدنى النافع للبند (أ + ب + ج، §7).**

### 12.1 ما بُني

- **`V86__offers_quantity_and_gifts.sql`**: على `offer` خمسة أعمدة - `buy_quantity` و`get_quantity` و`get_percent`
  و`max_per_invoice` و`quantity_limit` - و`offer_kind_chk` يُحذف ويُكتب كاملًا بالنوعين الجديدين، كل فرع يكتب
  IS NOT NULL لما يستعمله وNULL لما لا يخصه، و`offer_limits_chk`؛ وعلى `offer_target` عمود `role`
  (`QUALIFY`/`REWARD`) بـCHECK أن الهدية صنف لا مجموعة ولا استثناء؛ وعلى `sales` و`sales_re` عمود
  `offer_quantity` - كم وحدة من السطر غطّاها العرض - بـCHECK كأخيه، **وسطور V85 تُملأ** بما غطّاه عرضها فعلًا:
  السطر كله، بوحدته حين يسمّي العرض وحدة وإلا بوحداته الأساسية. والـtriggers التي تقرأ الأعمدة الجديدة قسم V86
  **في آخر** `R__triggers.sql`، يعيد تعريف أربعة triggers قسم V85: schema عند V85 لا يحمل الأعمدة، وMySQL يرفض
  trigger يسمّي عمودًا ليس فيه. والـview سطرا البيع والمرتجع يقرآن `offer_quantity`.
- **المحرك** (`OfferEngine`): النوعان المجمَّعان يعدّان سطور الصنف معًا بوحدة العرض أو بالوحدات الأساسية - كرتونة
  و3 قطع خمس عشرة قطعة (ق-ع٦) - **كل صنف على حدة**. الترتيب ق-ع٥: «اشترِ وخذ» ثم الكمية ثم عروض السعر، وداخل
  النوع الأولوية ثم الأنفع ثم الأقدم. خصم المجموعة يُوزَّع على سطورها **بالقيمة** والقرش الباقي على أكبرها (ق-ع٢).
  «3 بـ 100» يأخذ الوحدات الأغلى أولًا ولا يرفع سعرًا؛ و«اشترِ وخذ» من الصنف نفسه يعطي **أرخص** وحدات المجموعة؛
  والهدية تُعطى على سطرها بسعرها وخصمها (ق-ع٣) - ولا تُضاف وحدها، وغيابها **تلميح**. والتلميحات ثلاثة: ما يُكمل
  مجموعة، والوحدة التي يعطيها «اشترِ وخذ» لمن اشترى ما يكفي، والهدية المكتسَبة الغائبة.
- **الحدّان يعدّان «مرات» العرض**: وحدة لعروض النسبة والمبلغ والسعر، ومجموعة لعرضي الكمية. حد الفاتورة يُوزَّع على
  سطور عرض السعر بترتيبها. **والحدّ الكلي يُقرأ من السطور لا من عدّاد**: مجموع `offer_quantity` على سطور البيع
  - غير المستند المعدَّل - ناقص سطور المرتجع، على حجم المجموعة (`OfferService.timesLeft`). **الحفظ يقفل صف العرض
  ثم يقرأ السطور قراءة مقفلة (`FOR SHARE`)**: القراءة العادية تحت REPEATABLE READ ترى لقطة المعاملة من أول قراءة،
  والحفظ يقرأ أشياء كثيرة قبل العروض، فكانت ستفوّت بيعًا أكمله جهاز آخر وهي تنتظر القفل. والشاشة تقرأ بلا قفل،
  معاينةً يحكمها الحفظ.
- **الحفظ** (`InvoiceOffers.judge`): يسأل `timesLeft` مقفلًا ويحكم بالمحرك، و`OfferGuard` يقارن الآن عدد الوحدات
  المغطاة أيضًا (`Claim.offerQuantity`) - الحدّ الكلي يعدّها، فسطر يكتب عددًا خطأ كان سيُفسد العدّ.
- **المرتجع** يأخذ من سطر مصدره حصته من الوحدات المغطاة بنسبة كميته، فيعيدها إلى الحدّ الكلي.
- **تحذير ما تحت التكلفة** يحكم على **مرة كاملة من العرض** - مجموعة كاملة، أو ما يُشترى مع هديته - بالمحرك نفسه
  (`OfferEngine.givenAlone`)، ويسرد الهدية مرة واحدة.
- **الشاشات**: نموذج العرض بصف للكمية (عدد الوحدات، أو «اشترِ»/«وخذ»/«بخصم %») وصف للحدّين بسطر يشرح العدّ،
  وخانة «هدية» في محرر الأهداف لنوع «اشترِ وخذ»؛ والقائمة تكتب القيمة بكلمات بين الأرقام («اشترِ 2 وخذ 1 مجانًا»،
  «6 بـ 30.00») لأن «2 + 1» في خلية من اليمين تُقرأ «1 + 2»؛ والدرج يعرض الحدّين وما استُعمل من الكلي؛ والفاتورة
  - الشاشتان - تحت سطورها شريط التلميحات (`InvoiceOfferHints`، بلا JavaFX وله اختبار).

### 12.2 ما اختلف عن الخطة، ولماذا

| # | ما كُتب | ما بُني | لماذا |
|---|---|---|---|
| 1 | عرض الكمية على هدف (قد يكون مجموعة) | **كل صنف يُعدّ على حدة** | «3 بـ 100» على مجموعة بأسعار مختلفة لا جواب واحدًا له؛ الخلط بين الأصناف («أي 3 بـ 100») قرار مستقل - سجل الأفكار |
| 2 | كل قطعة يأخذها عرض واحد | والسطر يسمّي عرضًا واحدًا: **عرض مجمَّع يغطي جزء سطر يأخذ السطر كله**، والجزء الباقي بلا عرض | السطر يحمل `offer_id` واحدًا؛ إعطاء الباقي عرضًا آخر يعني شطر السطر |
| 3 | «العروض ثم الأصناف» ترتيب القفل | **الأصناف ثم العروض** | حارس المخزون يقفل الأصناف قبل الحكم على العروض، وحفظ الفاتورة هو المسار الوحيد الذي يقفل الاثنين، فالترتيب واحد |
| 4 | «حتى 3 قطع» و«أول 100 كرتونة» | الحدّان **يعدّان مرات العرض**: وحدات لعروض السعر، ومجموعات لعرضي الكمية | عدّ واحد يصلح للأنواع الخمسة، و«3 بـ 100 مرتين للعميل» جملة صحيحة |
| 5 | لم تحدد الخطة كيف يُعدّ المستعمَل | **مشتق من السطور** (`offer_quantity`)، لا عدّاد مخزَّن | القاعدة الأولى في هذا المشروع: رصيد يُشتق ولا يُخزَّن؛ الحذف والتعديل والمرتجع يصحّحونه بلا كود |
| 6 | «عرض الكمية لا يمسّ سطر الميزان» | لم يُبنَ | السطر لا يعرف أنه وُزن؛ الكمية الكسرية تُعدّ كما هي |
| 7 | «سطور هدية مقترحة» | **تلميح** يسمّي الهدية وكميتها | إضافة سطر بلا يد الكاشير تُخرج بضاعة لم يطلبها أحد |
| 8 | - | الأغلى أولًا في المجموعة، والأرخص يُعطى في «اشترِ وخذ» | سطور صنف واحد بوحدتين قد تختلف أسعار وحداتها؛ هذا هو ما يفعله المحل حين يعطي «الأرخص مجانًا» |
| 9 | - | المرتجع يعيد حصته من الوحدات المغطاة إلى الحدّ الكلي | «ما بيع منه ناقص ما رُدّ» (ق-ع١٠) |

### 12.3 ما شُغِّل على MySQL

- **`OfferDatabaseAcceptanceTest` خمس عشرة حالة** - السبع السابقة وثماني للمرحلة ج - **خضراء مرتين** على خادم MySQL
  8.0.31 خاص، ولم يبقَ schema: V86 من الصفر وCHECKاته (3819) والـtrigger على حدّ عرض مستعمَل (1644)؛ **المثال 2**
  (سبع عصائر بـ40: 40 خصمًا وست وحدات مغطاة، و`document_profit` 240 − 175)؛ **المثال 4** (الشامبو والبلسم: 24 و16،
  والربح −15، ومرتجع الشامبو وحده يرُدّ 36 بعرضه وحصته)؛ **المثال 3** (خمسة: 30 وثلاث وحدات) و**حدّ الفاتورة**
  يرفض سطرًا يدّعي مجموعتين والعدّاد لم يتحرك؛ **الحدّ الكلي بمعاملتين حقيقيتين**: الأولى تقفل العرض وتنتظر، والثانية
  - بعد قراءة عادية تثبّت لقطتها كما يفعل الحفظ - تنتظر القفل ثم ترى مجموعة الأولى بالقراءة المقفلة، **وترى القراءة
  العادية في المعاملة نفسها ما قبلها** - وهو ما يجعل `FOR SHARE` ضرورة لا احتياطًا؛ ثم عبر الحفظ: مجموعة باقية تُعطى
  مرة، والفاتورة التالية التي رأت الشاشة لها مجموعة تُرفض والعدّاد لم يتحرك؛ و**الترقية**: قاعدة عند V85 عليها بيعان
  بعروض ترتقي إلى V86 وكل سطر بوحداته (24 لنسبة على كرتونتين، و3 لعرض الكرتونة).
- **والجيران على الخادم نفسه خضر - 118 حالة في أربعة عشر صنفًا مع العروض في التشغيلة الثانية**: العملات (12)،
  مستندات العملة (11)، سقف الخصم (7)، نشاط المندوب (9) وتفصيله (6)، المصروفات (19)، باريتو (4)، ملف الطرف (7)،
  الشرائح (8)، قائمة الدخل (5)، مبيعات الأصناف (5)، الملخص (5)، أسباب المرتجعات (5)؛ ومصدر المرتجع (1) ومستودع
  المرتجعات (14) - وهما يقرآن وحدات سطر المصدر - على schema عند V86 لم يبقَ فيه صف.

### 12.4 ما شوهد على الشاشة

على schema عند V86 محمَّل بمحل الدليل الوهمي، بمستخدمَين عاديين، عند 1366×768: **عرضان كُتبا بأدوات النموذج وزر
حفظه** («بركة 6 بـ 30» بحد مجموعتين للفاتورة، و«اشترِ 2 شاي وخذ سكر هدية» بحد كلي 50) وقرأتهما القاعدة كما
كُتبا؛ **نافذة ما تحت التكلفة** للهدية تسرد الشاي والسكر عند `سعر3` (50 و29) بصافٍ 38.76 و22.48؛ والقائمة بأنواعها
وقيمها. **وفاتورة الكاشير**: سبع مياه بمجموعة واحدة (6.00)، وثلاثة تلميحات تحت السطور - أضف 5 مياه، وأضف زبادي
تأخذه بالعرض، والسكر هدية غائبة - ثم يُضاف السكر فيُوزَّع 31 على الشاي والسكر 23.88 و7.12، والحفظ بالزر عبر شاشة
الدفع يكتب في القاعدة ما حُسب باليد. **والفاتورة السريعة**: خمس مياه مُسحت بالباركود، والتلميح «أضف 1» فوق سطر الحالة.

**وما وجدته الشاشة وحدها، وأُصلح:** **شريط التلميحات أظهر أولها مقصوصًا بثلاث نقاط وأخفى الباقي** - الجدول بجواره
يأخذ كل الارتفاع ويضغط الـlabel إلى سطر؛ صار حدّه الأدنى ارتفاع نصه كله، وأُعيد التصوير فظهرت الثلاثة. وسطر شرح الحدّين في النموذج بلغ حافته،
فأخذ الحماية نفسها.

### 12.5 ما لم يُرَ

الإنجليزية والوضع الداكن؛ طابعة حقيقية (أسطر العروض على الورقة لم تتغير عن المرحلة ب)؛ تعديل عرض مستعمَل من النموذج
بخاناته الجديدة مقفلة؛ ورفض حدّ الفاتورة على الشاشة (أُثبت عبر الحفظ في اختبار القبول). وخانات الأرقام في النموذج
تكتب «2.0» و«100.0» - منسّق الأرقام القديم على كل خانة إدخال في البرنامج، لا من هذه المرحلة.
