سأقوم بمتابعة الخطوات بالانتقال إلى **المرحلة 3 (التقارير والطباعة)** — وهي الأكثر فائدة فورياً حسب التوصية في التوثيق.

دعني أولاً أستكشف البنية الحالية للمشروع لفهم المكتبات والأنماط المستخدمة.الممتاز! المرحلة 1 و 2 بالفعل منفّذة في المشروع. دعني أتأكد من وضع الملفات الحالية قبل البدء في **المرحلة 3 (التقارير والطباعة)** التي هي الخطوة التالية المنطقية.الآن أفهم السياق. المرحلتان 1 و 2 مكتملتان، والمستخدم يريد إكمال الخطوات. بناءً على التوصية في التوثيق، الخطوة التالية هي **المرحلة 3 (التقارير والطباعة)** ثم 4، 5، 6.

دعني أفحص البنية الحالية للتقارير والطباعة في المشروع لكي أضع خطة تنفيذ دقيقة:الآن لدي فهم كافٍ للمشروع ومساره. دعني أستعرض أيضاً بنية FXML للوردية الحالية قبل أن أوضح الخطة التالية.الآن لدي فهم جيد للوضع. دعني أبدأ المرحلة 3 (التقارير والطباعة). أحتاج أولاً أن أفهم بنية Service و DAO أكثر لفهم كيفية الوصول لـ `userShiftService`.ممتاز، لدي الآن صورة واضحة عن الحالة الحالية (المرحلتين 1 و 2 مكتملتان). سأقدم **خطة تنفيذ المرحلة 3 — التقارير والطباعة** بخطوات عملية ومحددة، مع كود جاهز للبدء بها.

---

# 🚀 المرحلة 3 — التقارير والطباعة

## 📋 نظرة عامة على الخطوات

| # | الخطوة | الأولوية | الملفات |
|---|--------|:-------:|---------|
| 1 | إنشاء `ShiftReportService` لتجميع بيانات التقارير | 🔴 عالية | جديد |
| 2 | إضافة مسارات Jasper للتقارير الجديدة | 🔴 عالية | `JasperReportPaths.java` |
| 3 | إضافة دوال الطباعة في `Print_Reports.java` | 🔴 عالية | تعديل |
| 4 | إنشاء قوالب Jasper (`.jrxml`) للـ Z-Report و X-Report | 🔴 عالية | جديد |
| 5 | ربط زر "طباعة X-Report" في الـ FXML + Controller | 🟡 متوسطة | تعديل |
| 6 | الطباعة التلقائية عند الغلق (Z-Report) | 🟡 متوسطة | تعديل |
| 7 | تقرير تجميعي للورديات (بفترة زمنية) | 🟢 منخفضة | جديد |
| 8 | تصدير Excel للورديات | 🟢 منخفضة | جديد |

---

## الخطوة 1️⃣ — إنشاء `ShiftReportService`

هذا الـ Service مسؤول عن **تجميع** بيانات التقرير في صورة جاهزة للطباعة (DTO + Map).

```java
package com.hamza.account.service;

import com.hamza.account.model.dao.DaoFactory;
import com.hamza.account.model.domain.ShiftSummary;
import com.hamza.account.model.domain.UserShift;
import com.hamza.controlsfx.database.DaoException;

import java.time.LocalDateTime;
import java.util.List;

/**
 * خدمة تجميع بيانات تقارير الورديات (X-Report / Z-Report / تقارير تجميعية).
 */
public record ShiftReportService(DaoFactory daoFactory, UserShiftService userShiftService) {

    /**
     * بيانات تقرير X (لحظي) — وردية مفتوحة.
     */
    public ShiftReportData buildXReport(int userId) throws DaoException {
        UserShift shift = userShiftService.getOpenShift(userId);
        if (shift == null) {
            throw new DaoException("لا توجد وردية مفتوحة لهذا المستخدم!");
        }
        ShiftSummary summary = userShiftService.getCurrentShiftSummary(userId);
        return new ShiftReportData(shift, summary, LocalDateTime.now(), "X-Report");
    }

    /**
     * بيانات تقرير Z — بعد غلق الوردية (يُستدعى بـ shiftId).
     */
    public ShiftReportData buildZReport(int shiftId) throws DaoException {
        UserShift shift = daoFactory.userShiftDao().loadById(shiftId);
        if (shift == null) {
            throw new DaoException("الوردية غير موجودة!");
        }
        ShiftSummary summary = ShiftSummary.builder()
                .openBalance(shift.getOpenBalance())
                .totalSales(shift.getTotalSales())
                .totalSalesReturns(shift.getTotalSalesReturns())
                .totalExpenses(shift.getTotalExpenses())
                .totalDeposits(shift.getTotalDeposits())
                .totalWithdrawals(shift.getTotalWithdrawals())
                .invoicesCount(shift.getInvoicesCount())
                .build();
        return new ShiftReportData(shift, summary, shift.getCloseTime(), "Z-Report");
    }

    /**
     * تقرير تجميعي لورديات فترة زمنية معينة.
     */
    public List<UserShift> buildAggregateReport(LocalDateTime from, LocalDateTime to, Integer userId)
            throws DaoException {
        return daoFactory.userShiftDao().getShiftsBetween(from, to, userId);
    }

    /**
     * DTO موحّد للتقرير.
     */
    public record ShiftReportData(
            UserShift shift,
            ShiftSummary summary,
            LocalDateTime printTime,
            String reportType
    ) {
    }
}
```


> ⚠️ هذه الدوال تتطلب إضافة `loadById` و `getShiftsBetween` في `UserShiftDao` إن لم تكن موجودة.

---

## الخطوة 2️⃣ — إضافة مسارات Jasper

```java
// ... existing code ...
    public static class Barcode {
        public static final String VERSION_1 = BASE_PATH + "barcode-one-label.jrxml";
        public static final String VERSION_2 = BASE_PATH + "barcode-two-label.jrxml";
        public static final String ITEMS = BASE_PATH + "items-barcode-A4.jrxml";
    }

    public static class Shift {
        // تقرير غلق الوردية — طباعة حرارية 80mm
        public static final String Z_REPORT_80 = BASE_PATH + "shift-z-report-80mm.jrxml";
        // تقرير لحظي — طباعة حرارية 80mm
        public static final String X_REPORT_80 = BASE_PATH + "shift-x-report-80mm.jrxml";
        // تقرير تجميعي A4
        public static final String AGGREGATE_A4 = BASE_PATH + "shift-aggregate-A4.jrxml";
    }
}
```


---

## الخطوة 3️⃣ — إضافة دوال الطباعة في `Print_Reports`

```java
// ... existing code ...
import com.hamza.account.model.domain.*;
import com.hamza.account.otherSetting.BarcodeDetails;
import com.hamza.account.service.ShiftReportService.ShiftReportData;
import com.hamza.account.view.LogApplication;
// ... existing code ...

    public void printBarcode(String barcode, String name, String price, int copies) {
        // ... existing code ...
    }

    // ==================== Shift Reports ====================

    /**
     * طباعة تقرير X (لحظي) - 80mm حراري.
     */
    public void printShiftXReport(ShiftReportData data) {
        HashMap<String, Object> map = buildShiftReportMap(data);
        jasperData.printJasperPrint(
                JasperReportPaths.Shift.X_REPORT_80,
                "X-Report", map, 1, printerNameThermal);
    }

    /**
     * طباعة تقرير Z (غلق) - 80mm حراري.
     */
    public void printShiftZReport(ShiftReportData data) {
        HashMap<String, Object> map = buildShiftReportMap(data);
        jasperData.printJasperPrint(
                JasperReportPaths.Shift.Z_REPORT_80,
                "Z-Report", map, 1, printerNameThermal);
    }

    /**
     * طباعة تقرير تجميعي لورديات متعددة - A4.
     */
    public void printShiftAggregateReport(List<UserShift> list, String from, String to, String username) {
        HashMap<String, Object> map = getStringObjectHashMap(list, null);
        map.put("dateFrom", from);
        map.put("dateTo", to);
        map.put("username", username == null ? "الكل" : username);
        addHeaderToReports(map, "تقرير الورديات");
        jasperData.printJasperPrint(
                JasperReportPaths.Shift.AGGREGATE_A4,
                "تقرير الورديات", map, 1, "");
    }

    private HashMap<String, Object> buildShiftReportMap(ShiftShiftReportDataAlias) {
        // (placeholder - see real helper below)
        return new HashMap<>();
    }

    private HashMap<String, Object> buildShiftReportMap(ShiftReportData data) {
        HashMap<String, Object> map = getCompany();
        var shift = data.shift();
        var summary = data.summary();
        double expected = summary.getExpectedBalance();
        double diff = summary.calculateDifference(shift.getCloseBalance());

        map.put("reportType", data.reportType());
        map.put("printTime", data.printTime().format(DATE_TIME_FORMATTER));
        map.put("shiftId", shift.getId());
        map.put("username", shift.getUsername());
        map.put("openTime", shift.getOpenTime() == null ? "" : shift.getOpenTime().format(DATE_TIME_FORMATTER));
        map.put("closeTime", shift.getCloseTime() == null ? "-" : shift.getCloseTime().format(DATE_TIME_FORMATTER));
        map.put("openBalance", shift.getOpenBalance());
        map.put("closeBalance", shift.getCloseBalance());
        map.put("totalSales", summary.getTotalSales());
        map.put("totalSalesReturns", summary.getTotalSalesReturns());
        map.put("totalExpenses", summary.getTotalExpenses());
        map.put("totalDeposits", summary.getTotalDeposits());
        map.put("totalWithdrawals", summary.getTotalWithdrawals());
        map.put("invoicesCount", summary.getInvoicesCount());
        map.put("expectedBalance", expected);
        map.put("difference", diff);
        map.put("notes", shift.getNotes() == null ? "" : shift.getNotes());
        return map;
    }
}
```


> 🧹 احذف الدالة الـ placeholder ذات التوقيع `buildShiftReportMap(ShiftShiftReportDataAlias)` — كانت فقط لإظهار المكان.

---

## الخطوة 4️⃣ — قالب Jasper مبسط لـ Z-Report (80mm)

أنشئ الملف في `reports/ar/shift-z-report-80mm.jrxml` كهيكل أولي:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<jasperReport xmlns="http://jasperreports.sourceforge.net/jasperreports"
              name="shift-z-report-80mm" pageWidth="226" pageHeight="842"
              columnWidth="206" leftMargin="10" rightMargin="10"
              topMargin="10" bottomMargin="10">

    <parameter name="reportType"       class="java.lang.String"/>
    <parameter name="printTime"        class="java.lang.String"/>
    <parameter name="shiftId"          class="java.lang.Integer"/>
    <parameter name="username"         class="java.lang.String"/>
    <parameter name="openTime"         class="java.lang.String"/>
    <parameter name="closeTime"        class="java.lang.String"/>
    <parameter name="openBalance"      class="java.lang.Double"/>
    <parameter name="closeBalance"     class="java.lang.Double"/>
    <parameter name="totalSales"       class="java.lang.Double"/>
    <parameter name="totalSalesReturns" class="java.lang.Double"/>
    <parameter name="totalExpenses"    class="java.lang.Double"/>
    <parameter name="invoicesCount"    class="java.lang.Integer"/>
    <parameter name="expectedBalance"  class="java.lang.Double"/>
    <parameter name="difference"       class="java.lang.Double"/>
    <parameter name="notes"            class="java.lang.String"/>

    <title>
        <band height="480">
            <!-- Header -->
            <textField><reportElement x="0" y="0" width="206" height="20"/>
                <textElement textAlignment="Center">
                    <font size="12" isBold="true"/>
                </textElement>
                <textFieldExpression>$P{reportType}</textFieldExpression>
            </textField>

            <line><reportElement x="0" y="22" width="206" height="1"/></line>

            <!-- بيانات الوردية -->
            <staticText><reportElement x="0" y="28" width="70" height="15"/>
                <text><![CDATA[المستخدم:]]></text></staticText>
            <textField><reportElement x="70" y="28" width="136" height="15"/>
                <textFieldExpression>$P{username}</textFieldExpression></textField>

            <staticText><reportElement x="0" y="45" width="70" height="15"/>
                <text><![CDATA[رقم الوردية:]]></text></staticText>
            <textField><reportElement x="70" y="45" width="136" height="15"/>
                <textFieldExpression>$P{shiftId}</textFieldExpression></textField>

            <staticText><reportElement x="0" y="62" width="70" height="15"/>
                <text><![CDATA[الفتح:]]></text></staticText>
            <textField><reportElement x="70" y="62" width="136" height="15"/>
                <textFieldExpression>$P{openTime}</textFieldExpression></textField>

            <staticText><reportElement x="0" y="79" width="70" height="15"/>
                <text><![CDATA[الغلق:]]></text></staticText>
            <textField><reportElement x="70" y="79" width="136" height="15"/>
                <textFieldExpression>$P{closeTime}</textFieldExpression></textField>

            <line><reportElement x="0" y="100" width="206" height="1"/></line>

            <!-- الإجماليات -->
            <staticText><reportElement x="0" y="108" width="120" height="15"/>
                <text><![CDATA[إجمالي المبيعات:]]></text></staticText>
            <textField pattern="#,##0.00"><reportElement x="120" y="108" width="86" height="15"/>
                <textFieldExpression>$P{totalSales}</textFieldExpression></textField>

            <staticText><reportElement x="0" y="125" width="120" height="15"/>
                <text><![CDATA[المرتجعات:]]></text></staticText>
            <textField pattern="#,##0.00"><reportElement x="120" y="125" width="86" height="15"/>
                <textFieldExpression>$P{totalSalesReturns}</textFieldExpression></textField>

            <staticText><reportElement x="0" y="142" width="120" height="15"/>
                <text><![CDATA[المصروفات:]]></text></staticText>
            <textField pattern="#,##0.00"><reportElement x="120" y="142" width="86" height="15"/>
                <textFieldExpression>$P{totalExpenses}</textFieldExpression></textField>

            <staticText><reportElement x="0" y="159" width="120" height="15"/>
                <text><![CDATA[عدد الفواتير:]]></text></staticText>
            <textField><reportElement x="120" y="159" width="86" height="15"/>
                <textFieldExpression>$P{invoicesCount}</textFieldExpression></textField>

            <line><reportElement x="0" y="180" width="206" height="1"/></line>

            <!-- الرصيد -->
            <staticText><reportElement x="0" y="188" width="120" height="15"/>
                <text><![CDATA[الرصيد الافتتاحي:]]></text></staticText>
            <textField pattern="#,##0.00"><reportElement x="120" y="188" width="86" height="15"/>
                <textFieldExpression>$P{openBalance}</textFieldExpression></textField>

            <staticText><reportElement x="0" y="205" width="120" height="15"/>
                <text><![CDATA[الرصيد المتوقع:]]></text></staticText>
            <textField pattern="#,##0.00"><reportElement x="120" y="205" width="86" height="15"/>
                <textFieldExpression>$P{expectedBalance}</textFieldExpression></textField>

            <staticText><reportElement x="0" y="222" width="120" height="15"/>
                <text><![CDATA[الرصيد الفعلي:]]></text></staticText>
            <textField pattern="#,##0.00"><reportElement x="120" y="222" width="86" height="15"/>
                <textFieldExpression>$P{closeBalance}</textFieldExpression></textField>

            <staticText><reportElement x="0" y="239" width="120" height="15">
                <textElement><font isBold="true"/></textElement>
                </reportElement>
                <text><![CDATA[الفرق:]]></text></staticText>
            <textField pattern="#,##0.00"><reportElement x="120" y="239" width="86" height="15">
                <textElement><font isBold="true"/></textElement>
                </reportElement>
                <textFieldExpression>$P{difference}</textFieldExpression></textField>

            <line><reportElement x="0" y="260" width="206" height="1"/></line>

            <staticText><reportElement x="0" y="270" width="60" height="15"/>
                <text><![CDATA[ملاحظات:]]></text></staticText>
            <textField isStretchWithOverflow="true"><reportElement x="0" y="287" width="206" height="40"/>
                <textFieldExpression>$P{notes}</textFieldExpression></textField>

            <textField><reportElement x="0" y="340" width="206" height="12">
                <textElement textAlignment="Center">
                    <font size="8"/>
                </textElement>
                </reportElement>
                <textFieldExpression>$P{printTime}</textFieldExpression></textField>
        </band>
    </title>
</jasperReport>
```


> كرّر نفس الهيكل لملف `shift-x-report-80mm.jrxml` مع تغيير العناوين (تقرير لحظي بدلاً من غلق) وإخفاء حقل "الرصيد الفعلي"/"الفرق".

---

## الخطوة 5️⃣ — ربط زر X-Report في الـ FXML

```xml
<!-- ... existing code ... -->
                <!-- ==================== Live Summary (X-Report) ==================== -->
                <TitledPane collapsible="true" expanded="true" text="ملخص الوردية اللحظي (X-Report)">
                    <GridPane alignment="CENTER" hgap="15" vgap="8">
<!-- ... existing code ... -->
                     </rowConstraints>
                    </GridPane>
                </TitledPane>
                <Button fx:id="btnPrintXReport" styleClass="btn-info" text="🖨️ طباعة تقرير لحظي (X)" />

                <!-- Close Shift Section -->
<!-- ... existing code ... -->
```


---

## الخطوة 6️⃣ — الطباعة التلقائية عند الغلق في الـ Controller

```java
// ... existing code ...
import com.hamza.account.model.domain.ShiftSummary;
import com.hamza.account.model.domain.UserShift;
import com.hamza.account.openFxml.FxmlPath;
import com.hamza.account.reportData.Print_Reports;
import com.hamza.account.service.ShiftReportService;
import com.hamza.account.session.ShiftContext;
// ... existing code ...

    @FXML
    private Label labelSummaryTotalSales, labelSummaryReturns, labelSummaryExpenses,
            labelSummaryExpected, labelSummaryDifference, labelSummaryInvoices;
    @FXML
    private Button btnPrintXReport;

    private final ShiftReportService shiftReportService;
    private final Print_Reports printReports;

    public UserShiftController(DaoFactory daoFactory) throws Exception {
        super(daoFactory);
        this.currentUserId = LogApplication.usersVo.getId();
        this.shiftReportService = new ShiftReportService(daoFactory, userShiftService);
        this.printReports = new Print_Reports();
    }
// ... existing code ...

    private void setupActions() {
        btnOpenShift.setOnAction(e -> openShift());
        btnCloseShift.setOnAction(e -> closeShift());
        if (btnPrintXReport != null) {
            btnPrintXReport.setOnAction(e -> printXReport());
        }
    }
// ... existing code ...

    private void printXReport() {
        try {
            var data = shiftReportService.buildXReport(currentUserId);
            printReports.printShiftXReport(data);
        } catch (DaoException e) {
            log.error("Error printing X-Report", e);
            AllAlerts.alertError(e.getMessage());
        }
    }

    private void closeShift() {
        try {
// ... existing code ...
            String notes = safeTrim(txtCloseNotes.getText());
            int closedShiftId = userShiftService.closeShift(currentUserId, closeBalance, notes);
            if (closedShiftId > 0) {
                ShiftContext.clear();
                // طباعة Z-Report تلقائياً
                try {
                    var zData = shiftReportService.buildZReport(closedShiftId);
                    printReports.printShiftZReport(zData);
                } catch (Exception ex) {
                    log.error("Error auto-printing Z-Report", ex);
                }
                AllAlerts.alertSaveWithMessage("تم غلق الوردية بنجاح!");
                clearCloseShiftFields();
                refreshView();
            }
        } catch (DaoException e) {
// ... existing code ...
```


> ⚠️ **ملاحظة:** `userShiftService.closeShift` حالياً يُرجع نتيجة `update()` وليس الـ `id`. يجب تعديل التوقيع ليُرجع `openShift.getId()` بعد الحفظ، أو إضافة دالة `getLastClosedShift(userId)`.

```java
// ... existing code ...
    public int closeShift(int userId, double closeBalance, String notes) throws DaoException {
// ... existing code ...
        if (notes != null && !notes.isBlank()) {
            String current = openShift.getNotes();
            openShift.setNotes((current == null || current.isBlank())
                    ? notes
                    : current + " | [غلق] " + notes);
        }

        daoFactory.userShiftDao().update(openShift);
        return openShift.getId();
    }
// ... existing code ...
```


---

## الخطوة 7️⃣ — تقرير تجميعي (UI مستقل)

أنشئ نافذة جديدة بسيطة:

- **FXML:** `shift-reports-view.fxml` — تحتوي `DatePicker` (من/إلى) + `ComboBox` للمستخدمين + `Button` عرض + `Button` طباعة + `TableView`.
- **Controller:** `ShiftReportsController` — يستدعي `shiftReportService.buildAggregateReport(...)` ثم `printReports.printShiftAggregateReport(...)`.

---

## الخطوة 8️⃣ — تصدير Excel (اختياري لهذه المرحلة)

استخدم `ReportExportService` الموجود مسبقاً في المشروع:

```java
reportExportService.exportToExcel(shiftsList, "shifts.xlsx", columns);
```


---

# ✅ خلاصة ما عليك فعله بالترتيب

1. **ابدأ بالخطوة 1** (إنشاء `ShiftReportService`) وتأكد أن `UserShiftDao` تحتوي `loadById` و `getShiftsBetween`.
2. **الخطوة 2 و 3** (إضافة المسارات + دوال الطباعة) — كود بسيط.
3. **الخطوة 4** — صمّم قوالب Jasper؛ يُفضَّل استخدام **JasperSoft Studio**.
4. **الخطوة 5 و 6** — اربط الـ UI واختبر التدفق الكامل.
5. **أجِّل الخطوة 7 و 8** إن أردت إنهاء الأساسيات أولاً.

---

## 💡 نصائح هامة

- ⚙️ **اختبر قالب Jasper يدوياً** في JasperSoft Studio قبل ربطه بالكود.
- 🖨️ تأكد أن `setting_printer_thermal` مُعَدّ في إعدادات البرنامج، وإلا ستظهر نافذة اختيار الطابعة.
- 🧪 **سيناريو اختبار شامل:**
    1. افتح وردية → اضغط "طباعة X" → تأكد من ظهور الملخص.
    2. أغلق الوردية → تأكد أن Z-Report طُبع تلقائياً بدون تدخل.
    3. افتح نافذة التقرير التجميعي → اختر فترة → اطبع.

---

### 📝 هل تريد أن أكمل بـ:

1. **كتابة `UserShiftDao.loadById` و `getShiftsBetween`** (الدوال الناقصة)؟
2. **قالب Jasper كامل لـ X-Report و التقرير التجميعي**؟
3. **إنشاء `ShiftReportsController` + FXML كامل**؟
4. **الانتقال مباشرة للمرحلة 4** (الصلاحيات والإدارة)؟